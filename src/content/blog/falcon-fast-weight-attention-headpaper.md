---
title: "FALCON 论文完整中文翻译（仅中文+图片）"
description: "基于 HeadPaper Skills 的完整版中文翻译，仅保留中文正文与原文图片（PNG）。"
date: 2026-03-18
author: "OpenAGI"
authorHandle: "openagi_ai"
tags: ["paper", "headpaper", "falcon", "translation"]
image: "/blog/falcon-2026-03-18/figures/figure-page-p002.png"
---

# FALCON 论文完整中文翻译（仅中文+图片）

> 输出规则：仅保留中文翻译与原文图片（PNG），移除无关元数据。

## 论文原图

![figure-page-p002.png](/blog/falcon-2026-03-18/figures/figure-page-p002.png)

![figure-page-p007.png](/blog/falcon-2026-03-18/figures/figure-page-p007.png)

![figure-page-p008.png](/blog/falcon-2026-03-18/figures/figure-page-p008.png)

## 完整中文翻译

### 第 1 页

**P0001** F ALCON：快速重量关注
持续学习
固件 A 作者
2026 年 3 月 9 日
摘要
状态空间模型（SSM）和快速权重存储器通过以下方式实现恒定成本解码
将历史记录压缩为固定大小的循环状态，但本地写/忘记规则通常是
启发式地对待。根据这里采用的先写后读约定，有一个简单的
索引陷阱：以标准 Transformer 约定编写的递归绑定（kt，vt），
而用于下一个令牌建模的因果对齐内部快速记忆训练对是
(kt−1, vt)，相当于标准索引下的(ki, vi+1)。我们进行下一个潜在配对
显式贯穿整个过程并将状态更新投射为在线学习：在观察stept之后，
循环状态对因果对(kt−1, vt)执行更新，然后被读取以支持
stept+1 处的预测。
项目页面：https://github.com/yifanzhang-pro/FALCON
1 简介
Transformers (Vaswani et al., 2017) 是现代自然语言无处不在的支柱
处理、支持基础大型语言模型 (LLM)，例如 GPT-4（Achiam 等人，
2023）、Gemini（Team 等人，2023）和 Llama（Touvron 等人，2023）。变压器的功效
源于自注意力机制，该机制模拟全局依赖关系以生成丰富的、
上下文感知的表示。然而，这种能力是有代价的：标准注意力量表
二次O(N 2)，序列长度为N。这种复杂性给
长上下文处理，其中注意力矩阵和内存所需的计算
键值 (KV) 缓存的带宽变得令人望而却步。
除了效率之外，长上下文建模也是一个持续学习的问题：模型需要
快速记忆可以在线绑定新证据，同时避免来自无限的灾难性干扰
积累。 Transformer 将这种快速内存外部化为不断增长的键值 KV 缓存。 SSM
另一方面，快速权重模型将其压缩为固定大小的循环状态。在这个
设置时，状态更新规则充当局部学习规则，其时间对齐决定
快速记忆是否根据预测时实际可用的信息进行训练（Sun
等，2024；刘等人，2024a； Behrouz 等人，2024；王等人，2025）。
诸如 Linear Attention (Katharopoulos et al., 2020)、Fast Weight Programmers (Schlag et al., 2021)、RWKV (Peng et al., 2023) 和 Mamba (Gu and Dao, 2023) 等架构
1

### 第 2 页

**P0002** 潮气量
圣
×
写
之前的工作
（启发式关联）
kt↔vt
kt−1
St−1线性
预测因子 × ˆvt L
VT
特征文本目标
St←St−1
(I−λtDiag(ηt))+xt
(ηt⊙rt
)⊤,
rt :=vt−S⊤t−1xt, ηj,t= βj,t
∥xt∥22+λt+ε。
梯度步骤我们的：FALCON-2
（在线回归）
kt−1→vt
下一个潜在预测
St−1
kt−1
kt−2
窗口B
VT
vt−1
× ˆVt Σ
F ALCON-3:St←(1−ηtλt)St−1+ ηt
|它|
Σ
j∈It
xj
(vj−S⊤t−1xj
)⊤,
F ALCON-3A:St←γtSt−1+ρt ́N(B)t ,
N(B)t := 1
|它|
Σ
j∈It
xjv⊤j, γt := 1−αt。
小批量GD
FALCON-3 / FALCON-3A
窗口→∇
A. 时间不匹配 B.自回归下一个潜在预测
C. 可并行滑动窗口更新
图1 概念概述。(A) 许多快速权重规则bind(k t, vt)（缓存式关联）。下
用于下一个令牌建模的预测快速记忆目标，步骤对 a 中揭示的因果示例
将新观察到的目标作为特征前缀，产生(kt−1, vt)。 (B)FALCON-2（下一个潜在预测）：
stateSt−1 根据前缀 featurext :=phi(kt−1) 预测 v t，然后执行在线岭回归
更新。在完整的 FALCON-2 中，NLMS 增益为每列： ηj,t =βj,t/(∥xt∥2
2 +λt +ε)，所以两者都更新
幅度和收缩因子 1−λtηj,t 是逐列的（相当于，每个输出特征/列
圣）。这也保持了符号的一致性：βj,t 是增益，而 ηj,t 是实际的归一化步长。
(C) 滑动回归 (ATLAS (Behrouz et al., 2025a),FALCON-3) 应用小批量回归步骤
在标称大小 B 的活动窗口It（实现的 sizeBt :=|It|≤B）上，在因果对 (xj, vj) 上
xj := phi(kj−1)。其对应的内积 FALCON-3A 使用相同的窗口平均互协方差
́N(B)
t 而是一个解耦的写入系数ρt 和衰减分数αt。
2

### 第 3 页

**P0003** 在中小型体系的选定基准上与变形金刚具有竞争力，同时
在训练中保持 O(N) 缩放比例，在推理过程中保持每步 O(1) 复杂性。
尽管取得了这些进步，但管理循环状态的更新规则，即模型如何编写
记忆中的记忆和遗忘，仍然主要是启发式的。此外，一旦确定了严格的
本文使用先写后读语义，存在一个简单的索引陷阱：许多重复
是根据标准 Transformer 约定编写的，因此是 bind(kt, vt)。在此之下
惯例，stept 中揭示的因果训练示例将新观察到的 targetvt 与
在stept−1 中计算的前缀特征。同样，在标准 Transformer 索引下，
预测关联是(ki, vi+1)。我们使这种依赖于约定的下一个潜在配对变得明确
整个过程。
Titans 和 ATLAS 将循环状态视为通过优化内部训练来训练的快速记忆
流上的目标（Behrouz 等人，2024，2025a）。我们的贡献是正交的：我们使
自回归训练显式配对（写后读下的 kt−1→vt）并导出与 SSD 式块并行训练保持兼容的封闭形式一阶更新（Dao 和 Gu，2024）。在
特别是，F ALCON-3 在滑动窗口上使用单个小批量梯度步骤。它的回归
形成关联扫描/并行流展开，而其内积对应物 F ALCON-
3A 承认隐藏注意力的展开。相比之下，许多线性注意力/SSD 公式依赖于
纯加性内积写入。
在这项工作中，我们通过自回归的视角探讨了基于状态的建模的基础
下一个潜在预测。我们将状态更新形式化为显式在线优化问题
并明确我们的先读后写下一个潜在约定所需的一步转换。在我们的
框架中，循环状态矩阵St充当线性预测器，尝试映射前缀
写出 featurext := phi(kt−1)tov t。通过分析 SSM 和线性注意力作为在线优化器，
我们确定不同的目标函数、平方误差回归与负内积
目标（相当于非正则化情况下的内积最大化），驱动他们
各自的动态。我们的贡献如下：
• 我们将循环状态解释为通过在线学习规则更新的快速权重。的
增益 βt 控制可塑性（Liu et al., 2024a），岭系数 λt 导致遗忘，并且
滑动窗口变体实现了有界排练信号，减轻了长距离
干扰。
• FALCON-2：我们从归一化最小均方导出改进的 Delta 更新规则
（NLMS）算法。通过引入输入范数归一化和每列自适应
增益和衰减，F ALCON-2 明确考虑了不同的输入规范和异构
输出特征动态，同时保持并行效率。
• FALCON-3：我们通过小批量将回归目标推广到滑动状态机制
SGD，产生有限窗口优化信号，减轻远程干扰和
使用可并行算法进行噪声累积，以实现硬件友好的训练和推理。
这与ATLAS中引入的Omega学习规则密切相关（Behrouz et al., 2025a），
它在滑动上下文窗口上优化内部记忆目标，而不仅仅是
最新的令牌。
FALCON-3A：我们引入了与滑动状态对应的内积，将块并行训练与滑动窗口累积规则和解耦参数化相结合
可塑性和遗忘的影响：写入系数ρt控制注入幅度，而a
无量纲衰减分数αt 控制遗忘。
3

### 第 4 页

**P0004** 2 背景
2.1 状态空间模型
状态空间模型 (SSM)（Gu 和 Dao，2023；Dao 和 Gu，2024）处理一系列输入
x(t)εRdin 通过压缩的潜在状态h(t)εRn，产生输出y(t)εRdout。的
连续时间线性动力学由下式控制
˙h(t) =A(t)h(t) +B(t)x(t), y(t) =C(t) ⊤h(t),
其中B( t)∈Rn×din 和C( t)∈Rn×dout （所以C( t)⊤h(t) 是dout 维的）。离散操作
序列中，这些动态在步长Δt上离散化。在零阶保持假设下，
由此产生的离散时间系统具有精确的封闭式解。
为了避免混淆离散化方案，我们使离散时间映射变得明确。结束
步长Δt，标准形式为
ht = Atht−1+ Btxt, At = e Δ tAt, Bt =
∫ Δ t
0
e(Δt−s)AtBtds.(2.1)
当A t 是对角线时，输入积分 ́Bt 承认元素闭形式：
́Bt =
（
A−1
t (eΔtAt−I)
）
BT,
解释A−1
t (eΔ tAt−I) 逐元素并使用极限(eΔa−1)/a→Δas a→0。一阶
近似值为 ¯Bt≈ΔtBt（当∥ΔtAt∥小时有效），得出
ht≈eΔ tAtht−1+ ΔtBtxt。
现代选择性 SSM，例如 Mamba（Gu 和 Dao，2023），对系统矩阵进行参数化
(B, C, Δ) 作为当前输入 xt 的函数，允许内容感知过滤。 Mamba-2（道
和Gu，2024）进一步将转移矩阵A简化为标量或对角结构，建立
称为结构化状态空间对偶（SSD）的理论桥梁。这种二元性表明
循环 SSM 相当于因果线性注意力的特定形式，从而实现高效
通过块并行矩阵乘法进行训练，同时保持恒定状态推理。
2.2 线性注意力
线性注意力（Katharopoulos et al., 2020）规避了标准的 O(N 2) 复杂性
通过用内核特征图 phi(·) 替换 softmax 来引起注意：Rd→Rm 使得 κ(qt, kj) =
Φ(qt)⊤Φ(kj)。利用矩阵乘法的结合性，第 t 个输出yt∈Rdv
令牌是：
yt =
Σt
j=1ψ(qt)⊤ψ(kj)vj
Σt
j=1 Φ(qt)⊤Φ(kj) =
(Σt
j=1ψ(kj)v⊤
j
)⊤
Φ(qt)
ψ(qt)⊤Σt
j=1(kj) ,(2.2)
该公式允许将上下文压缩为循环矩阵状态St∈Rm×dv并且
归一化器zt∈Rm：
yt = S⊤
t φ(qt)
z⊤
t ψ(qt) +εattn
,S t =S t−1+ψ(kt)v⊤
t ,z t =z t−1+ψ(kt).(2.3)
4

### 第 5 页

**P0005** 这里εattn≥0是一个小稳定器。仅当读取规范化器时才使用规范化形式
是非负的，例如对于正特征图，其中 phi(·)≥0 元素，因此 z⊤
t ψ(qt)≥0。在
签名特征设置（包括没有额外正性约束的恒等映射），
εattn 只防止被零除，并且不会使分母类似注意力；在这种情况下，
下面介绍的无分母形式是数学上相关的默认值。
因果关系和索引。 (2.3) 遵循标准 Transformer 约定：在positiont我们
从更新后的状态（St，zt）中读取，所得表示用于预测tokent+1。
我们的下一个潜在对齐将写入流移动一位：在观察vt之后，我们将其写入
先前的 keykt−1，或等效地，在标准索引下 i =t−1 下的 (ki, vi+1)。这产生
写后读循环
yt = S⊤
t φ(qt)
z⊤
t ψ(qt) +εattn
,S t =S t−1+ψ(kt−1)v⊤
t ,z t =z t−1+ψ(kt−1),(2.4)
其中εattn≥0是一个小稳定剂。定义移位密钥流~kt :=k t−1, 方程（2.4）匹配
等式。 (2.3) 将kt 替换为~kt 后；实质性的变化是键值对齐(~kt,vt)。
归一化与非归一化线性注意力。方程中的分母。 (2.2) 使用归一化器
statez t 重新调整读数。许多 SSM/SSD 风格的架构反而会降低分母
(andz t) 并使用非标准化内积读取：
yt =S⊤
t ψ(qt),S t = (1−ηtλt)St−1+ηtψ(kt−1)v⊤
，（2.5）
通过用 k t 替换 kt−1 来恢复未移位的约定。在这种无分母的形式中，
有界状态幅度和可控记忆时间尺度是通过显式衰减来强制执行的（例如，
λt> 0) 和/或增益控制。 4.3节表明加性更新正是梯度下降
内积目标。
这个递推是第 4.3 节中内积目标的梯度下降更新。
当 λt = 0 时，写入纯粹是加性秩一赫布学习；当λt > 0时，是可加的
加上标量收缩。如第 2.1 节所述，Mamba-2 证明无归一化器重现是
功能上相当于SSD框架下的特定类别的SSM。
2.3 台达网络
Fast Weight Programmers 和 Delta Networks (Schlag et al., 2021) 将序列建模公式化为
价值检索函数的在线学习。设St−1∈Rd×dv表示快速权状态矩阵。
标准 Delta Networks 采用错误驱动更新，而不是纯粹的累加累积
从状态重建之间的瞬时平方误差的梯度导出
当前键和值：
ℓt(S) := 1
2
S⊤kt−vt

2
2
.(2.6)
这里，S⊤kt 表示模型对给定 keykt 的 valuevt 的预测。相对于的梯度
状态为 ∇Sℓt(S) =k t
（
S⊤kt−vt
）⊤。
步长约定。我们用 ηt 表示梯度步长（之前的 DeltaNet 文献经常写道
该数量为βt)。这使得符号与第 3 节保持一致，其中 βt 表示 NLMS 增益
5

### 第 6 页

**P0006** 和 ηt 得到的步长。步长为 ηt 的在线梯度下降的单步得出
德尔塔规则：
St =S t−1−ηt kt(S⊤
t−1kt−vt)⊤= (I−ηtktk⊤
t )St−1+ηtktv⊤
t.(2.7)
一阶因子(I−ηtktk⊤
t ) 沿当前关键点方向执行有针对性的收缩/编辑。
仅在特殊情况下，它才变成子空间正交 tokt 上的正交投影
ηt = 1/∥kt∥2
2. 这与线性注意力的纯加性更新形成对比。
门控 Delta 网络。门控 Delta 网络的最新研究（Yang 等人，2024a）观察到
Delta规则的隐式遗忘完全取决于kt和状态之间的对齐，
这可能不足以快速进行上下文切换。他们将曼巴风格的门控与
通过引入显式衰减门αt∈[0,1] 的 delta 规则：
St =g t(I−ηtktk⊤
t )St−1+ηtktv⊤
，（2.8）
其中我们写 gt∈[0, 1]（Yang 等人（2024a）中表示为 αt 的门）以避免符号过载。
这种门控 Delta 规则提供了两种互补机制：gt 允许全局状态衰减
（与 SSM 类似），而一级项 (I−ηtktk⊤
t ) 对键值执行有针对性的编辑
协会。
3 自回归下一个潜在预测
在本节中，我们将循环状态更新形式化，而不是作为历史的启发式积累，
但作为一个显式优化问题。根据此处采用的先写后读约定，
在线目标必须在时间上与下一个令牌预测保持一致。标准达美网络
最小化∥S⊤kt−vt∥2，即，它们在对（kt，vt）上进行训练。根据我们的惯例，因果训练
stept 中显示的示例将新观察到的 targetvt 与现有的前缀特征配对
当v t 被预测时（在stept−1）。这产生了下一个潜在配对kt−1→vt（等价地，
标准 Transformer 索引下的 ki→vi+1）。
我们假设隐藏状态S充当在线训练的线性预测器来映射kt−1→vt。
考虑一个键序列{kt}T
t=1 和值{vt}T
t=1。我们将状态更新制定为在线
岭回归问题（Behrouz 等人，2024；Wang 等人，2025；Behrouz 等人，2025a）。
在stept处，写特征必须是从前缀到t−1可计算的，而写特征必须是可计算的
只有在显示 stept 后，目标才可用。因此，我们的下一个潜在对齐配对
前一个键与当前值，xt ≜ phi(kt−1)andy t ≜ vt （其中 phi 中的恒等式
非核化情况），并通过在线岭回归步骤进行更新（Wang 等人，2025；Behrouz
等，2024、2025a）。 steptis 处的瞬时正则化最小二乘目标：
ℓt(S)≜ 1
2
S⊤xt−yt

2
2
+ λt
2∥S∥2
F,(3.1)
其中λt≥0是正则化系数。同时全批次最小化累积损失
对应于 MesaNet (von Oswald et al., 2025) 等方法中找到的离线解决方案，高效
自回归建模需要在线近似。因此我们采用在线梯度
血统（OGD）。
符号。在核化线性注意力中，写入内存的密钥通常是特征向量
ψ(k)εRm 而不是原始密钥 kεRd。本节中的所有推导均经过解释
xt = phi (kt−1) ∈ R m , S t ∈ R m × d v ,
6

### 第 7 页

**P0007** 所以我们将 x t 视为通用写入特征（出现在更新和更新中的向量）
标准化器）并保持符号整洁。用于检索的查询，例如线性注意力中的 phi(qt)，
生活在相同的特征空间中，但不必相等。
隐式快速记忆目标。 (3.1) 是瞬时目标，其梯度步长定义
快速内存写入规则；它不是外部自回归之外的额外监督损失
可能性。在训练期间，我们通过更新来区分，以便慢速权重（其中
Produce(q , k, v) 以及 βt,λt) 学习进行这些本地更新的表示和门
有用的。
自回归快速权重更新
RBW Prefix1:t−1 READSt−1 预测观测值
目标vt
写测试
kt−1
千吨
平移：(kt−1,vt)
未平移：(kt,vt)
RA W kt−1 观察者
目标vt
写 读 预测+1
xt
yt
stept 处的因果对(xt,yt)
平移（我们的）：xt = phi(kt−1),yt = vt
未平移：xt = phi(kt),yt = vt
捕获的前缀 1 :t−1
RBW：读然后写
RA W：先写后读（用于F ALCON）
配对消融
RBW 原始数据
kt（未移位）
kt−1（平移）
运行 1（原始） 运行 2
跑 3 跑 4（我们的）
图 2 正式时序约定。上图：我们明确区分先读后写（RBW）和
写后读（RAW）自回归快速权重时序语义。根据 RAW 约定
F ALCON，stept 处的因果对齐训练对是(k t−1, vt)，即与新的前缀特征配对
显露目标。下图：时序约定 {RBW,RAW} 和写入配对 {kt, kt−1} 上的 2×2 消融
检查收益是否来自因果时间调整而不是单独的索引。
索引和因果关系约定。我们按当前上下文位置索引时间：在positiont，
7

### 第 8 页

**P0008** 该模型消耗了 token1:t 并预测 tokent+1。我们使用先读后写的顺序：after
观察t，我们将状态更新为St，然后从St读取以形成位置处的表示
t。在下一个潜在对齐下，timet 处的更新使用因果对(kt−1, vt)（等效地，
(ki, vi+1) 在标准 Transformer 索引下，i =t−1)。图2中的四个单元分别对应
不同的局部目标约定。根据此处采用的 RAW 约定，移位配对
(kt−1, vt) 是与预测 vt 时可用的前缀特征对齐的特征。的
未移位的 RAW 配对（kt，vt）仍然是下一个标记预测的因果关系，但它优化了不同的
基于同步特征而不是前缀特征的局部目标。同样，转移后的
RBW 配对本身并不不一致，但它在产生读取后训练内存
在positiont处进行预测，因此与本文分析的内部预测器不匹配。我们
初始化S 0 =0，并采用全纸边界约定x1 :=0（等价地， phi(k0) =0
对于 ZeroBOSwrite 功能）。在此默认零状态预填充下，t = 1write 是无操作。如果
跨段携带非零状态，并且希望边界步骤是严格的无操作
即使当 λ1 > 0 时，也设置 η1 := 0。为了简单起见，我们编写递归 fort≥2。有了这个
约定，stept 处的下一个潜在预测是 ˆyt :=S⊤
t−1xt 并且更新使用残差
rt =y t−ˆyt。这个 ^yt 是本地写丢失使用的内部快速内存预测；应该
不要与positiont处的模型读数混淆，后者在写后读取下由
更新后的状态（例如 ot =S⊤
t qt 在无分母的情况下）。等价地，^yt 是预测
在stept−1结束时可用，来自当前状态St−1和前缀featurext = phi(kt−1)；
在 t 被揭示后，我们更新到 St，并且位置 t 处的读数使用 St 来预测 tokent+1。在
实现中，我们可以选择在形成（q，k，v）之前应用一个短因果卷积，其中
与之前的研究结果一致，提高了局部联想回忆（Fu et al., 2022；Liu et al., 2024b）。
快速记忆是持续学习。状态是在线更新的快速记忆（快速权重）
在前向传递中，慢速权重学习表示和门，使这些
本地更新很有用。从持续学习的角度来看，每个标记都提供了一个新的训练示例
(xt, yt)：写入增益控制可塑性，收缩/衰减路径控制遗忘，而
第 4 节中的滑动窗口变体实现了有界排练，以减少远程干扰。
对于回归式变体，这种遗忘自然地通过岭系数λt来表达；为了
内积滑动状态变体F ALCON-3A 我们改为参数化无量纲
直接衰减分数（第 4.5 节），它是实际控制递归的数量。
方程（3.4）、（4.8）和（4.18）中的归一化是关键的稳定器，使更新具有规模鲁棒性
跨越异构特征规范并防止长序列上的灾难性漂移。
单位矩阵。我们将特征空间中的 dx×dx 恒等式写为 I dx ，其中 dx ≜dim (xt)
（sodx =d 表示原始密钥，dx =m 表示内核化写入功能），andIdv 表示 dv×dv 身份
价值空间。当维度明确时，我们写I。
3.1 在线梯度下降更新
方程中瞬时损失的梯度。 (3.1) 关于状态：
∇Sℓt(S) =x t(S⊤xt−yt)⊤+λtS.(3.2)
8

### 第 9 页

**P0009** 应用具有学习率 ηt 的单个梯度下降步骤产生更新规则：
St←St−1−ηt∇Sℓt(St−1)
=S t−1−ηt
[
xt(S⊤
t−1xt−yt)⊤+λtSt−1
]
= (1−ηtλt)St−1+ηtxtr⊤
t ,(3.3)
其中 t ≜ yt−S⊤
t−1xt 是残差（预测误差）。请注意，与标准 Delta Networks 不同，
rt 测量前一个 keykt−1 的预测与当前的预测之间的差异
值 v t.
这是瞬时脊目标上的梯度下降。使用标准化步长
下面，就变成常态化更新了。在特殊情况下 λt = 0 且 ε= 0，它精确地减少为
经典的 NLMS 递归，因为 ε>0 它是通常的稳定 NLMS 变体。具体来说，
损失 ℓt 相对于 Frobenius 范数是 Lt 平滑的，平滑常数 Lt =∥xt∥2
2+λt。
为了确保数值稳定性和尺度鲁棒性，我们采用归一化步长：
ηt = βt
∥xt∥2
2 +λt +ε, β t∈(0,2), ε≥0.(3.4)
当 ∥xt∥2 时，我们采用约定 ηt := 0
2 +λt +ε= 0。分析时，可取ε= 0且
假设Lt> 0（等效地，∥xt∥2> 0或λt> 0）。在实现中，我们取ε>0作为数值
鲁棒性；任何 ε > 0 只会减少 ηt，因此保留下面的下降保证。
增益与步长的关系。我们用 βt∈(0, 2) 表示无量纲 NLMS 增益，用 etat 表示
结果（标准化）步长。一些先前的 DeltaNet 工作使用 βt 来表示原始步长，我们的
符号将这两个量分开。更准确地说，在未钳位 ε= 0 分析中，βt > 1
沿当前写入特征方向产生负特征值（第 3 节），这种机制可以
改善状态跟踪行为（Grazzi et al., 2024）。
将岭解释为衰减。对于 λt> 0，岭项在 中引入标量因子 γt := 1−ηtλt
等式。 （3.3）。然而，完整的线性转变为At =γtIdx−ηtxtx⊤
t ：它的特征值γt为
子空间正交 toxt 和特征值γt−ηt∥xt∥2
2 = 1−ηt(∥xt∥2
2 +λt) 沿 x t。当 ε= 0 且
NLMS步长Eq.(3.4)，该方向特征值等于1−βt，所以βt>1产生a（稳定）
即使 γt > 0，特征值也为负。当对数空间展开要求 γt > 0 时，我们实现
复发与
αt := min(ηtλt,1−εγ), γ t := 1−αt,
并计算 fp32 中的 logγt = log1p(−αt)。以下所有精确重整化恒等式都是精确的
对于这个实施的复发；当钳位处于非活动状态时 (ηtλt < 1−εγ)，它与
原γt = 1−ηtλt。本节中的 OGD/下降语句指的是未夹紧的
复发。如果钳位激活，则执行的更新不再是精确的梯度下降
式（3.1）上的岭系数λt；相反，它应该被解释为使用有效衰减
收缩路径中的系数 ～λt :=αt/ηt≤λt （当 ηt = 0 时，约定 ～λt := 0）。在
在默认的 RMS 标准化状态下，钳位器旨在保持不活动状态并且仅用作
数字保障。
3.2 分析
我们表明，归一化步长在瞬时正则化中产生每步下降
目标ℓt，基本的局部稳定性属性。这个语句是pointwise int：它并不意味着
9

### 第 10 页

**P0010** 累计在线损失单调递减Σ
sℓs 或外部自回归训练
客观。
引理3.1（平滑损失的每步下降）。设f：Rdx×dv→Rbe L-平滑
到弗罗贝尼乌斯范数。对于任意步长 ηε(0, 2/L)，梯度步长 S+ =S −η∇f(S) 满足
f(S+)≤f(S)−η(2−ηL)
2 ∥∇f(S)∥2
F.(3.5)
证明.L-光滑度，对于任意SandS′，
f(S′)≤f(S) +⟨∇f(S),S′−S⟩+L
2∥S′−S∥2
F。
设S′=S + =S−η∇f(S)并化简。
步长参数化。选择 η= β/L 和 βε(0, 2)（因此要求 L >0）产生
减少系数β(2−β)/(2L)。对于L>0，稳定选择η=β/(L+ε)且ε≥0也
位于(0, 2/L)。当L = 0时，该区间未定义；在此处出现的退化情况中（xt =0
且 λt = 0，或相应的加窗模拟），梯度为零，因此我们定义更新
通过设置 η:= 0 成为无操作。
3.3 Delta 网络作为回归
在本节中，我们将在此优化框架下解释许多先前的快速权重模型。
我们观察到 Delta Networks 和 Linear Attention 不仅仅是架构启发法，而且
特定在线目标函数的解决方案。
通过代入回归分配xt←ψ(kt−1)和dy t←vt，方程（3.3）恢复
Delta 网络更新规则的函数形式（Schlag 等人，2021），但具有关键索引
班次：
圣 =
（
(1−ηtλt)Idx−ηtxtx⊤
t
）

腐烂和有针对性的遗忘
St−1+ηtxty⊤
t
写
,x t = phi(kt−1),y t = v t.(3.6)
这里，一级项xtx⊤
t St−1 是平方误差损失的左 Hessian 作用
当前的写入功能方向。直观地说，它减少了当前预测器的分量
在添加新目标 yt = v t 之前，作用于 xt = phi(kt−1)。
相反，如果我们用内积目标替换回归（MSE）损失（第 4.3 节），
残差项消失，更新变成纯粹的累加。与标准（未移位）
赋值(x t, yt) = (phi(kt), vt)（或(k t, vt)在非核化情况下）这简化为熟悉的
线性Attention / Mamba-2积累；与我们的下一个潜在分配（xt，yt）=（ phi（kt−1），vt）
它产生了我们的方法使用的一步转换变体（例如第 4.5 节中的 FALCON-3A）。
这种回归观点激发了我们在第 4 节中分析的关键算法改进：
使用 NLMS 式归一化，有效步长缩放为 ηt∝(∥xt∥2
2 +λt)−1(减少到
∥xt∥−2
2 当 λt = 0 时）。这表明固定学习率对于回归式快速权重更新来说是尺度不匹配的，对于内积写入，相同的归一化最好被视为
幅度稳定器而不是曲率要求。
10

### 第 11 页

**P0011** 算法 1 通过 SGD 批量大小 1 进行在线 Ridge
需要：钥匙{kt}T
t=0，零 BOS 写入特征 phi(k0) =0，特征图 phi(默认身份)，
值{vt}T
t=1，岭系数{λt}T
t=1 且 λt≥0, 增益βt∈(0, 2), 稳定器ε≥0(设 ηt:=0
如果分母为0)，则initS0 =0。
1:堡垒= 1,...,Tdo
2:x t←phi(kt−1)▷写入特征（前缀，前一个键）
3:y t←vt ▷目标（当前值）
4: ^yt←S⊤
t−1xt ▷快速记忆预测
5:r t←yt−ˆyt ▷Residual（预测误差）
6:\eta t←0ift=1或∥xt∥2
2 +λt +ε= 0, 否则βt/(∥xt∥2
2 +λt +ε)▷归一化步长；这个
强制执行全纸边界无操作
7:S t←(1−ηtλt)St−1+ηt xtr⊤
t ▷更新状态
8：结束
4 FALCON：快速体重注意
快速权重（矩阵值）存储器和线性注意力变换器被广泛视为在线
在前向传递中更新循环记忆的模型，可追溯到经典的快速权重
记忆机制及其在线性 Transformers 中的现代实例化以及 Delta 式规则
辛顿和普劳特 (1987)；施米德胡贝尔（1992）；巴等人。 （2016）；施拉格等人。 （2021）。在本节中，
我们通过在线优化的角度分析状态空间模型和线性注意力。我们
区分两个主要目标函数：回归（平方误差）和负数
内积目标（相当于非正则情况下的内积最大化）。由
在我们的shiftedkt−1→vt框架下导出它们各自的在线更新规则，我们激励
具体改进，包括自适应学习率和滑动窗口公式。
4.1 缩放线性注意力和缩放DeltaNet
与 softmax 注意力不同，它使用缩放点积⟨q, k⟩/
√
d，快速体重复发是
对查询和键的规范直接敏感：（i）点积读取随着∥qt∥2∥k∥2增长，并且
(ii) 附加（内积）写入随着写入特征规范而增长。为了稳定两个read
和写入流，特别是在长解码范围和混合精度下，我们使用显式
特征缩放/标准化。
缩放特征。我们为向量 u∈Rdu 定义一个通用的 RMS 归一化运算符：
RMSNorm(u) := u
均方根（u）+εrms
,RMS(u) := 1√du
∥u∥2,
其中 εrms > 0 是一个小的稳定器。除非另有说明，我们将 RMSNorm 应用于 (qt, kt)
在形成点积或外积之前，快速权重读/写使用的投影。这个
默认值不同于常见的ℓ2-归一化 DeltaNet 变体（例如，在门控 DeltaNet 实现中），并且在混合精度方面更加稳定，因为 RMSNorm 保持坐标
幅度 θ(1)，同时保留 NLMS 解释，因为 RMSNorm 意味着 ∥u∥2
2≈du。
默认情况下，我们不对值进行标准化；值归一化 (VNorm) 是可选的且已禁用
除非明确启用。
11

### 第 12 页

**P0012** 缩放线性注意力。在下一个潜在对齐下，我们使用 RMS 归一化投影
（可选核化）特征空间。非标准化读数为
yt =S⊤
t ψ(qt),S t = (1−ηtλt)St−1+ηt xtv⊤
t ,x t := phi(kt−1)。
如果使用归一化变量，则匹配的分子/归一化器递归为
yt = S⊤
t φ(qt)
z⊤
t ψ(qt) +εattn
,S t =γtSt−1+ηt xtv⊤
t ,z t =γtzt−1+ηt xt, γ t := 1−ηtλt。
当 z 0≥0 和元素非负特征图时，这种归一化形式仍然类似于注意力
仅当所有 t γt≥0 时；否则，即使 phi(·)≥0，zt 也可以改变符号。因此，每当我们
使用归一化读取，我们强制执行正衰减钳位（等效于 ηtλt≤1−εγ），否则
使用无分母形式。
方程中的加性归一化情况。 (2.4) 由 ηt ^ 1 和 λ t ^ 0 恢复。
对数空间块局部重整化 (ctx λ)。在长上下文设置中，
衰减因子在混合精度下可能会变得数值脆弱。使用此重新参数化
仅当衰减为正时。定义
αt :=ηtλt, γ t := 1−αt,
并且，每当无法先验保证正性时，在计算之前夹紧αt←min(αt,1−εγ)
logγt := log1p(−αt)
在 fp32 中。在一段时间内，我们通过总和累积乘法衰减
logγt，即
t∏
我=一个
γi = exp
( t Σ
我=一个
对数γi
）
,
并对分子状态（例如 St）和归一化器应用相应的重新缩放
块内的状态（例如，zt）。对于标准化读数
yt = S⊤
t φ(qt)
z⊤
t ψ(qt) +εattn
,
准确的输出保留还需要在撤消常见的重新缩放后形成比率，
或者等效地，按相同因子重新调整稳定器。固定εattn>0并直接使用
比率内的重整化状态，等价性只是近似的。无分母
复发不受此警告的影响。
缩放 DeltaNet。对于回归式快速权重，我们在内部使用相同的缩放 write-featureext
NLMS 更新：
St = (1−ηtλt)St−1+ηt xt
（
vt−S⊤
t−1xt
)⊤, η t = β t
∥xt∥2
2+λt+ε。
为了保持缩放和未缩放实现之间的 λscale 具有可比性，我们参数化无量纲基脊
¯λt :=λscaleσ(~λt), λ eff
t := ¯λt∥xt∥2
2、
12

### 第 13 页

**P0013** 因此，在每个非滑动缩放变体中，网络生产的数量是无量纲比率
λt = λeff
t /∥xt∥2
2 而不是绝对山脊系数。进入循环的实际脊
是λeff
t。然后我们使用λeff
NLMS 分母和衰减路径中的 t：
ηt = βt
∥xt∥2
2+λeff
t + ε, γ t = 1−ηtλeff
t。
因此
ηtλeff
t = βt́λt
1 + ́λt +ε/∥xt∥2
2
,
并且，当ε= 0时，
ηtλeff
t = βt́λt
1 + λt
, η t xtx⊤
t = βt
1 + λt
xtx⊤
t
∥xt∥2
2
。
因此，当 ε= 0 时，标量衰减分数和投影项对于
均匀缩放xt↦→cxt；对于 ε > 0，这种不变性只是近似的，并且在
正常状态ε≪∥xt∥2
2. 这是更新使用的精确规模鲁棒性属性。确实如此
这并不意味着整个块实际上没有改变，除非读取路径一致地重新缩放
以及。对于滑动窗口变体，替换∥xt∥2
2 窗口平均写入能量 ¯E(B)
t。
同样，对于滑动窗口变体，我们使用
́E(B)
t：= 1
转氨酶
Σ
j∈It
∥xj∥2
2、 λ eff,(B)
t := ´λt ´E(B)
t。
因此，相同的解释继续存在：学习/基础量 ́λt 是无量纲的，而
实际滑窗脊为λeff,(B)
t。每当此比例耦合参数化处于活动状态时，
快速记忆目标中出现的实际系数、平滑常数、衰减分数和
血统陈述 isλeff
t (或 λeff,(B)
t 在滑动窗口的情况下），而 ́λt 只是有界的
网络产生的基本参数。在当前的滑动状态实现中，E(B)
这是
在形成 λeff 时，将其视为仅统计乘数，(B)
t（相当于，乘法器被分离
/ stop-gradient），而 NLMS 分母仍然使用 live¯E(B)
t。
尺度耦合岭参数化的范围。上面的讨论适用于回归样式更新（F ALCON-2 和 F ALCON-3），其中特征能量进入平滑度
界，并且岭项确实属于局部目标。它不应该被结转
与滑动内积更新 F ALCON-3A 保持不变。 InF ALCON-3A，特征能量
项仅用于标准化写入幅度，并且更自然地控制复发
通过写入系数和直接衰减分数（第 4.5 节）；在那里参数化原始山脊
不必要地将遗忘与当前写入幅度结合起来。
通过对数衰减和局部重正化进行 Ctxλ 减少。对于仿射的标量衰减重现
形式
圣 =
（
γtI−ηtxtx⊤
t
）
St−1+ηt xty⊤
t , γ t>0,
对于 t 写入目标（例如 vt），定义 这里γt 是任何正标量衰减。在未夹紧的情况下
递归 γt = 1 −ηtλt；对于实现钳位，我们改为使用 γt := 1 −αt 与
αt = min(ηtλt,1−εγ)。
c0 := 1, c t :=
t∏
r=1
γr，~St：=St/ct。
13

### 第 14 页

**P0014** 然后
〜St =
（
I−~ηt xtx⊤
t
) ～ St−1+ ～ t x t ～ y ⊤
t , ～ t := t t
γt
, 〜yt := yt
ct−1
。
因此，精确等价需要写入的步长重新缩放和逆衰减重新缩放
目标；单独调整 ηt 是不够的。在块并行实现中，我们不形成
全球产品ct；相反，我们在每个块内累积 logγt 并应用等效的局部
重整化为边界状态并写入目标。
4.2 回归损失（Delta 网络）
我们将状态更新表述为自回归线性回归问题。在时间步长，让
状态beSt−1∈Rdx×dv，写入特征bext≜phi(kt−1)∈Rdx，目标beyt≜vt∈Rdv。
（在非核化情况下， phi 是恒等式，dx = d。）状态St−1 充当线性预测器
将前缀 featureext 映射到新观察到的 targetyt。
瞬时平方误差损失定义为：
ft(St−1) = 1
2
S⊤
t−1xt−yt
2
2.(4.1)
令 t ≜y t−S⊤
t−1xt 表示残差。相对于状态的梯度为：
∇Sft(St−1) =x t(S⊤
t−1xt−yt)⊤=−xtr⊤
t.(4.2)
Delta 更新（标准与下一个潜在）。在线梯度下降 (OGD) 的一步
学习率 ηt 产生 Delta 更新：
St =S t−1−ηt∇Sft(St−1)
=S t−1+ηtxtr⊤
t
= (Idx−ηtxtx⊤
t )St−1+ηtxty⊤
t.(4.3)
在下一个潜在对齐下，我们有 xt = phi(kt−1) 和 t = v t。将 kt−1 替换为 k t （等效地，
xt←phi(kt)) 恢复 Schlag 等人未移动的 DeltaNet 更新。 （2021）。
L2 正则化。添加 L2 惩罚 λt
2∥S∥2
F 为瞬时损失产生收缩
在线更新中的 term−ηtλtSt−1 （参见方程（3.3））。在标准化步长下，这会导致
乘法因子(1−ηtλt)，我们将其视为一种简单且可控的遗忘机制。
FALCON-2：自适应学习率。我们提出FALCON-2，它结合了NLMS风格
使用每通道自适应学习率进行归一化。在附录 D 中，我们推导了向量化对偶
使得列式自适应在 GPU 上易于计算处理。这允许步骤
size 为向量 ηt∈Rdv，调整每个值通道（S 列）的更新幅度
独立。
每列 NLMS 步长。具体来说，我们使用跨列共享的 NLMS 样式标准化器
并学习每列增益（相当于每个输出特征/列增益）：
ηj,t = βj,t
∥xt∥2
2 +λt +ε, β j,t ε (0,2), ε>0。
14

### 第 15 页

**P0015** 当 λt = 0、ε= 0 且增益跨通道绑定 (βj,t≠βt) 时，这可简化为经典模型
标量 NLMS 步长；否则，它是具有共享标准化器的按列 NLMS 泛化。
由于平方误差（岭）损失在值坐标（S 的列）上分解，因此每步
引理 3.1 中的下降参数按列应用，假设 0<βj,t<2 for allj。
更新规则。令 t = ( 1 , t , ... , dv , t ) ⊤ 。更新可以简洁地写为：
St =S t−1
（
Idv−λt Diag(ηt)
）
+xt
（
ηt⊙rt
)⊤,(4.4)
展开 r t = y t−S⊤
t−1xt 使回归引起的目标编辑变得明确：
St =S t−1
（
Idv−λt Diag(ηt)
）
−xt
（
x⊤
t St−1Diag(ηt)
）
+xt
（
ηt⊙yt
）⊤。
这突出显示了 FALCON-2 中的两种编辑/收缩机制：每列右乘
收缩（权重衰减路径）和沿当前写入功能的每列一级编辑
方向。
与 RWKV-7 和 Kimi Linear Attention.RWKV 型循环法学硕士的关系（Peng 等人，
2023）和更新的 delta 规则变体（例如 RWKV-7 和 Kimi Linear Attention（Team 等人，
2025)) 通过学习门控和上下文学习率控制丰富了 Delta 式快速权重。我们的
表述是互补的：我们将写入规则建立在因果下一个潜在回归目标中，
明确所需的一步关键转变，并通过 NLMS 归一化稳定学习。的
相同的标准化/对齐可以用作内部本地更新的直接替代
增量式块，独立于其特定的门控或混合参数化。
等效地，按列写入更新，其中 st,j 是 St 的第 j 列，(y t)j 是第 j 列
坐标ofyt，
st,j = (1−λt\etaj,t)st−1,j+\etaj,t xt
（
(yt)j−⟨xt,st−1,j⟩
）
.(4.5)
该公式允许模型通过 ηt 学习每列更新速度，而山脊
路径贡献每列收缩因子(1−λtηj,t)。
对数空间遗忘 (ctxλ) 和精确的列式缩减。对于未钳制的更新，
每列收缩因子为
γ原始
j,t := 1−λtηj,t。
因为允许的增益允许λtηj,t > 1，所以可以有γraw
j，t < 0；对数空间缩减
因此需要正衰减实现。每当我们在日志空间中展开时，我们使用
α原始
j,t :=λtηj,t, α j,t := min(αraw
j,t ,1−εγ), γ j,t := 1−αj,tε(0,1],
并计算 fp32 中的 logγj,t = log1p(−αj,t)。定义
c0,j := 1, c t,j :=
t∏
s=1
γs,j, ~st,j :=st,j/ct,j。
然后，实现的正衰减列递归等效于仅投影仪
形式
〜st,j =
（
I−~ηj,txtx⊤
t
) ～st−1,j+ ～ηj,t ～vt,j xt, ～ηj,t := ηj,t
γj,t
, ~vt,j := vt,j
ct−1,j
。
15

### 第 16 页

**P0016** 算法2并行化FALCON-2（Chunk-wise Forward，λt = 0）
要求：移位写入特征KεRL×dx，查询特征QεRL×dx，值VεRL×dv，每通道步长ηεRL×dv且ηt，j≥0。块大小C（假设C|L）。
确保：仅投影仪递归的输出 O ∈ RL × dv (λt = 0)。
1：将长度-L 轴划分为 M = L/C 连续块。对于每个块 m，让
K(m), Q(m) εRC×dx 和 V (m) εRC×dv 和 η(m) εRC×dv 表示通常的行优先
切片。
2：对于WY/Gram代数，使用列主块矩阵
K(m) :=K⊤
(m)∈Rdx×C,Q (m) :=Q⊤
(m)∈Rdx×C。
3:InitializeS in←0dx×dv ▷(或者提供的初始状态)
4：初始化输出缓冲器O←0L×dv。
5:形式= 1,...,Mdo
6:K←K(米),Q←Q(米),V←V(米),η←η(米)
7:Σ← √η▷ C×d v
8:G←K ⊤K▷ C×C(共享基 Gram)
9:M←tril(Q ⊤K,0)▷ C×C（未缩放分数；先写后读）
10:H←Q ⊤Sin,P←K ⊤Sin ▷ C×dv
11:▷对于每个通道j，构建L j =I+ tril(G⊙(Σ:,jΣ⊤
:,j),−1) 并求解Ljzj =Σ :,j⊙V:,j
且 L jγj =Σ :,j⊙P:,j
12：Z←批量求解Tri
（
{Lj}dv
j=1,V⊙Σ
）
▷ C×dv；专栏
13: ←BatchedSolveTri
（
{Lj}dv
j=1,P⊙Σ
）
▷ C×dv；柱jisγ j
14:B←Σ⊙(Z−Γ)▷ C×d v
15:O (m)←H+MB▷ C×d v（块输出）
16:S out←Sin +KB▷ d x×dv（块退出状态）
17:WriteO (m)←O(m) ▷存储到行(m−1)C+1 :mCofO
18:S in←Sout ▷传播到下一个块
19：结束
20：返回O。
因此，精确等价需要步长重新调整 ���j,t = �j,t/γj,t 和逆衰减
重新调整每列写入目标。当钳位处于非活动状态时 (λtηj,t< 1−εγ)，这与
与未夹紧的复发完全一致；否则，它是具有较小值的数字安全替代项
收缩路径中的有效衰减。在实践中，我们使用 chunk-local log-prefix 来实现这一点
衰减而不是全局产品ct,j；附录D给出了显式的内核。
块并行实现。FALCON-2 可以通过分块来进行序列并行训练
长度轴并在每个块内使用 Gram/WY 表示。对于每个通道的步长，
每个价值维度都有自己的单位下三角系统，但所有系统共享相同的
基本 Gram 矩阵G=K ⊤Kinside 一个块。算法 2 总结了分块前向传递
对于仅投影仪的情况（λt = 0）；附录 D 导出了这种对偶形式并讨论了 λt > 0
扩展。
16

### 第 17 页

**P0017** 4.3 内积损失（线性注意力和 Mamba-2）
我们现在考虑一个内积目标，它鼓励状态预测之间的一致性
和目标。我们以最小化形式编写它，并可选择添加 L2 惩罚：
ℓip
t (S)≜−⟨S⊤xt,yt⟩+λt
2∥S∥2
F，λt≥0。(4.6)
当 λt = 0 时，目标在 S 中是线性的（无有限极小值），梯度下降减少为
赫布写道，纯粹是加法。
标准与下一个潜在对齐。如果我们选择未移位的特征(x t, yt) = (phi(kt), vt)(或
(kt, vt)在无核化的情况下），下面的附加更新与通常的线性注意力相匹配
写 ψ(kt)v⊤
t（方程（2.3））。我们的下一个潜在框架使用(xt, yt) = (phi(kt−1), vt)，产生
一步移位的写入流。
FALCON-A variants.We refer to inner-product fast-weight instantiations asF ALCON-2A for the
非滑动更新（方程（2.5））和 F ALCON-3A 用于第 4.5 节中的滑动窗口更新。
梯度和更新。方程的梯度。 (4.6) 是
∇Sℓip
t (S) =−xty⊤
t+λtS。
具有rateηt的梯度步骤产生线性注意力/Mamba-2风格的更新
St = (1−ηtλt)St−1+ηt xty⊤
t ,(4.7)
其中x t = phi(kt−1)andy t = v t 在下一个潜在对齐下。设置λt=0恢复正常
加法 writeSt =S t−1+ηtxty⊤
t。
FALCON-2A步长。对于λt>0，ℓip
t 是 λt-smooth（它的 Hessian 矩阵是 λtI），所以任何 ηt∈(0, 2/λt) 都会产生
每步下降；重要的是，这个下降条件仅取决于 λt（曲率仅来自
来自岭项），而不是在 ∥xt∥2 上。 In practice, wealsonormalize by the write-feature energy to
控制附加 writexty⊤ 的大小
当特征规范变化时（例如，在内核映射下）
并获得良好的 λt→0 极限：
ηt = βt
∥xt∥2
2 +λt +ε, β t∈(0,2), ε≥0.(4.8)
如方程（3.4）所示，当分母消失时，我们设置 ηt := 0。当λt>0时，该选择满足
ηt≤βt/λt（当∥xt∥2 = 0且ε= 0时紧），因此对于任何βt∈(0, 2)，ηt < 2/λt。自从 ℓip
t
是 λt-smooth（它的 Hessian 矩阵是 λtI），引理 3.1 产生ℓip 中的每步下降
t 为该步长。的
∥xt∥2
2 项不是曲率所要求的，但可以稳定写入幅度并产生合理的
λt→0极限。当 λt = 0 时，ℓip
t 没有有限极小值；相同的归一化（λt = 0）应该
纯粹被解释为幅度稳定器（取ε>0，或当分母
消失）。
衰减正性。一些并行/展开形式（第 4.6 节）在对数空间中使用 γt := 1 −ηtλt
因此要求 γt > 0。在实现中，计算 αt := ηtλt，并在必要时钳制
αt←min(αt, 1−εγ) 在计算 logγt = log1p(−αt)(fp32) 之前。每当
ηtλt < 1−εγ; in that regime, the dynamics matchγt = 1−ηtλt exactly.如上所述，下降
声明涉及未夹紧的复发；当钳位激活时，更新应该是
被视为数字上安全的替代物，在收缩路径中具有较小的有效衰减。
17 号

### 第 18 页

**P0018** 4.4 回归的小批量更新规则（FALCON-3）
为了更好地捕获局部依赖性并减少噪声积累，我们引入了滑动状态
机制：我们不只根据瞬时残差更新状态，而是采用单个
标称大小的有限历史窗口上的小批量梯度步长 B。
与 ATLAS Omega 规则的关系。ATLAS (Behrouz et al., 2025a) 引入了 Omega 学习规则，
它通过执行基于梯度的内部目标优化来更新内存模块
在滑动上下文窗口上，而不是仅针对最近的标记进行优化。猎鹰3号
可以被视为这种滑动窗口原理对（i）线性矩阵存储器的特化
S，(ii) 平方误差回归目标，以及 (iii) 统一窗口权重。我们的配方
另外通过下一个潜在的对齐来强制执行严格的自回归因果关系（用
(kt−1, vt) 在先写后读的顺序下）并使用保守的跟踪归一化步长，
实现稳定的训练，同时保持与块并行扫描实现的兼容性。
精确的流状态。对于FALCON-3和FALCON-3A，矩阵状态St单独不是马尔可夫
为了准确的延续：下一次更新还取决于活动的 windowIt。精确延续
因此，除了 St 之外，跨段边界还需要固定宽度的边界尾部
包含最后 B−1 个因果对 {(xj, vj)}t
j=max(2,t−B+2)（或等效的滚动窗口
代表）。在段边界重置此尾部会更改边界附近的更新
并且应该被视为明确的边界重置，而不是相同边界的精确延续
复发。
序列并行训练。FALCON-3是等级至多B的低等级Delta规则（Omega规则）。
将每个活动窗口零填充到 widthB 后，循环在以下位置有一个固定的 B 级驱动程序
每一步。算法3给出了参考顺序更新。对于硬件高效的培训，我们
realize the same recurrence chunk-parallel with ParallelFlow’s triangular-tensor solver (tensorInv),
使用方程（4.15）中的精确正衰减减少。算法 4 给出了明确的伪代码
这个实施。在tensorInv内部，我们在(time,rank)对上使用block-strict-causal mask，所以
同时排名组件不会交互，并且每个残差都在更新前的状态下进行评估
St−1。然后，输出物化使用包容性块因果掩码来实现先写后读。
附录 F 推导了驱动程序的构造。内积对应物（FALCON-3A）承认
显式屏蔽线性注意形式（第 4.6 节），实现对
序列维度。
Fort≥2，令活动窗口索引为 ={j|max(2,t−B+ 1)≤j≤t} 并表示
实现的窗口大小为Bt :=|It|≤B(soBt≥1)。定义写入特征xj :=phi(kj−1)(so
xj =k j−1（当 phi 是恒等式时）。我们使用边界约定 x1 = phi(k0) = 0 并设置 η1 := 0，
所以 t = 1write 是一个空操作； all windowed objectives/updates below are defined fort≥2.使
更新幅度（以及有效衰减）对于标称窗口大小B不变，我们
优化窗口平均平方损失：
ℓreg,(B)
t (St−1) = 1
2Bt
Σ
j∈It
∥S⊤
t−1xj−vj∥2
2+λt
2∥St−1∥2
F,(t≥2).(4.9)
充分统计。我们定义滑动协方差C(B)
t 和互协方差N(B)
t 矩阵：
乙(B)
≜
Σ
j∈It
xjx⊤
j , N (B)
t ≜
Σ
j∈It
xjv⊤
(4.10)
18

### 第 19 页

**P0019** 定义窗口平均统计量
́C(B)
t：= 1
转氨酶
乙(B)
t , N(B)
t：= 1
转氨酶
乙（乙）
t。
那么窗口平均损失的梯度是
∇Sℓreg,(B)
t (St−1) = C(B)
t St−1−́N(B)
t+λtSt−1。
更新规则。我们应用块归一化梯度步骤：
St =S t−1−ηt∇Sℓreg,(B)
t (St−1).(4.11)
替换梯度产生仿射更新
圣 =
（
Idx−ηt( ́C(B) )
t+λtIdx)
）
St−1+ηt N(B)
t.(4.12)
同样，收集更新前状态的残差
St = (1−ηtλt)St−1+ ηt
转氨酶
Σ
j∈It
xj
（
vj−S⊤
t−1xj
)⊤.(4.13)
这是方程（3.3）的直接小批量模拟：所有窗口残差均在
预更新状态St−1，并且更新平均它们的一阶梯度。
Fort≥2，我们对窗口平均协方差使用保守的迹/能量归一化：
́E(B)
t := tr( ¯C(B)
) = 1
转氨酶
Σ
j∈It
∥xj∥2
2 = tr(C(B)
)
转氨酶
, η t = β t
́E(B)
t+λt+ε
, β t∈(0,2), ε>0。
由于 ℓreg,(B)
t 是 L(B)
使用 L(B) 进行 t 平滑
t = λmax( ́C(B)
t ) +λt 和 λmax( ́C(B)
t )≤tr( ́C(B)
t ) = E(B)
t ,
每当 L(B)
t > 0此归一化确保 ηt∈(0, 2/L(B)
t ) 对于任何 βt ∈ (0, 2)，所以引理 3.1
适用于平滑度L(B)
t 并保证每步下降。如果L(B)
t = 0（等价地，C(B)
时间=0
且 λt = 0), 则 ́N(B)
t = 0 也是如此，更新是无操作的。至关重要的是，因为我们优化了
窗口平均值，均为 ́N(B)
t 和 E(B)
t = tr( ¯C(B)
t ）是平均值而不是总和，因此它们的典型值
比例不随标称窗口大小而增长 B.如果写入功能本身是 RMS 归一化的
（例如， 是恒等式或近似保持范数），则 ¯E(B)
对于任意B，t ≈dx；对于一个通用的
kernel mapphi，正确的说法就是 ́E(B)
t 跟踪已实现的平均写入功能
能量。因此，无论是注入 ηt ¯N(B)
t 或衰减分数 αt :=ηtλt 是系统性的
通过增加而放大 B.如果启用了第 4.1 节的尺度耦合脊参数化，
将本小节中的 λt 替换为 λeff
t := ´λt ´E(B)
t。在当前的实现中，这
构建 λeff 时，窗口平均能量用作仅统计乘数
t（分离
/ 通过乘法器停止梯度），而步长分母仍然使用 live¯E(B)
t。
重要的是，不需要具体化 dx×dx 矩阵 C (B)
t 应用渐变：如果我们堆叠
窗口将特征写入Xt∈Rdx×|It|，然后
́C(B)
t St−1= 1
转氨酶
Xt
（
X⊤
t St−1
）
,(4.14)
19

### 第 20 页

**P0020** 算法 3FALCON-3（递归形式）
要求：查询特征{qt}T
t=1 且 qt = phi(qt)，移位写入特征{xt}T
t=1 且 x 1 =0 且 x t = phi(kt−1)
堡垒≥2，值{vt}T
t=1，窗口大小B，增益βtε(0,2)，脊λt≥0，稳定器ε>0，initS0 =0。
确保：OutputsO ∈RT×dv 且 rowt 也相等⊤
t=q⊤
t St（写后读），最终矩阵状态ST，以及
最终 FIFO 缓冲区WT；后续片段的精确延续需要 ST 和 W T。
1：初始化空的 FIFO 缓冲区W←[ ]，输出缓冲区O←0。
2:堡垒= 1,...,Tdo
3：ift=1然后
4:\eta t←0,S t←St−1,o t←S⊤
tqt
5：写⊤
t 进入 rowtofO 并继续
6：结束如果
7:将(x t,vt)推入W； if|W|>B，弹出最旧的对。
8:B t←|W|,Ut←0dx×dv,E t←0
9:对于所有(x,v)∈Wdo
10:Ut←Ut+x
（
v−S⊤
t−1x
)⊤
11:Et←Et+∥x∥2
2
12：结束
13：E（B）
t←Et/Bt
14:\eta t←\beta t/( ˆE(B)
t+λt+ε)
15:St←(1−ηtλt)St−1+(ηt/Bt)Ut
16:o t←S⊤
tqt
17：写⊤
t 进入 rowtofO
18：结束
19：返回（O，S T，W）
可以在 O(Bdxdv) 时间内评估窗口大小 B。对于实施的正衰减
重现，定义
γt := 1−αt, α t := min(ηtλt,1−εγ), c 0 := 1, c t :=
t∏
r=1
γr，~St：=St/ct。
然后，通过替换 1−ηtλt 从方程（4.13）中获得实现的正衰减递推
与γt，等价于
〜St = 〜St−1+ ^ t
转氨酶
Σ
j∈It
xj
( vj
ct−1
−～S⊤
t−1xj
)⊤
, ˆηt :=ηt/γt.(4.15)
在块[a,b]内，我们使用局部前缀δ(k)
0 := 1 和 δ(k)
i := ∏a+i−1
r=a γr 对于 i = 1,...,b−a+ 1,
所以传入的边界状态Sa−1 已经在局部重整化尺度中（空积
= 1).因此，参与step-t窗口的每个目标都共享相同的逆衰减因子
c−1
t−1 全局表示法，或等价的(δ(k)
当 t =a +i−1 时，i−1)−1 在 chunkk 内。算法4
通过日志前缀衰减在本地实现此身份块，这解释了为什么它重新缩放
整个第 t 个窗口值块除以一个公因子。对于更大的窗户和最大
与 SSD 式块并行性兼容，下面的内积公式甚至更便宜
因为它完全避免了这个术语。
边界注释。算法 3 是针对 x1 = 0 的新序列编写的。为了准确继续
在后面的段中，还必须携带最终的 FIFO 缓冲区 WT；最小边界有效负载是
最后 B−1 个因果对。仅通过 ST 会重置边界附近的滑动目标。
20

### 第 21 页

**P0021** 通过 ParallelFlow 的算法 4FALCON-3（块并行前向）
要求：查询{qt}T
t=1，键{kt}T
t=0 (withBOSkeyk 0), 值{vt}T
t=1, 特征图 phi(默认身份),
窗口大小B，增益βtε(0, 2)，脊λt≥0，稳定器ε>0，衰减钳εγ>0，块大小C（假设
C|T），初始化S0。
确保：OutputsO ∈RT×dv 且 rowt 也相等⊤
t = phi(qt)⊤St（先读后写），最终矩阵状态ST，
和边界 tailTT :={(xj,vj)}T
j=max(2,T−B+2) 用于在后面的段中精确延续。
1：写入/查询特征：x t← phi(kt−1)和q t← phi(qt)对于allt；设置x 1←0。
2：窗口索引I t←{j|max(2,t−B+ 1)≤j≤t}，窗口大小Bt←|It|，和B+
t←max(1,Bt)。
3：窗口平均能量： ¯E(B)
1 ←0且要塞≥2组 ́E(B)
t←1
转氨酶
Σ
j∈It∥xj∥2
2.
4：步长：setη1←0且fort≥2setηt←βt/( ¯E(B)
t+λt+ε)。
5：钳位衰减：αt←min(ηtλt,1−εγ)，logγt←log1p(−αt)，γt←exp(logγt)，和ˆηt←ηt/γt。
6：划分为M=T/Cchunks[a k,bk] = [(k−1)C+1,kC]。
7:▷阶段1：块内ParallelFlow解决（块上并行）
8:fork= 1toMin 并行 do
9:a←a k,b←bk,p←max(2,a−B+1),J←{p,...,b}。
10:切片Q (k)←(q⊤
)b
t=a∈RC×dx 并收集写入对{(xj,vj)}j∈J。
11：构建零填充窗口堆栈X(k)
win∈Rdx×(CB)和V(k)
win∈Rdv×(CB)：B列的第i块
包含从 Ia+i−1（以 j 递增）的对，用 0 填充到宽度 B。
12：局部对数前缀衰减（长度 C 扫描）：δ(k)
0 ←1,u0←0, 且 fori = 1:C setui←ui−1+ logγa+i−1,
δ(k)
我←exp(ui)。
13：按块重新缩放值：划分 blockiofV (k) 中的每一列
以标量δ(k)获胜
i−1，产生 ^V(k)
赢了。
14：▷扁平化时间×等级。同时等级组件是解耦的；仅较早的时间块相互作用
内部张量Inv.
15：驱动矩阵：标度块i ofX(k)
si 获胜 := ˆηa+i−1/B+
a+i−1得到A(k) ∈Rdx×(CB)；设置
R(k)←−X(k)
赢并且~R(k)←ˆV(k)
赢了。
16:(W(k),U(k))←张量Inv
（
A(k),R(k),~R(k))
▷（时间，等级）对上的块严格因果求解
17：可选扫描形式：M(k)←δ(k)
C
（
Idx+A(k)W(k))
且 b (k)←δ(k)
C A(k)U(k)。
18：缓存
（
Q(k),A (k),W (k),U (k),δ (k))
其中 δ (k) := (δ(k)
1 ,...,δ(k)
C）。
19：结束
20：▷阶段 2：块间边界传播（顺序；相当于关联扫描
(M(k),b(k)))
21：S (1)
在←S0。
22：叉= 1toMdo
23：商店（k）
在 .
24：Z(k)←U(k)+W(k)S(k)
在 .
25：S(k)
出←S(k)
+A (k)Z(k).▷ 本地重整域中的块退出
26:S (k+1)
在 ←δ(k)
CˆS(k)
out.▷ 恢复块边界处的原始比例；等效S(k+1)
=M(k)S(k)
在 +b (k) 中
27：结束
28:S T←S(M+1)
在 .
29：▷阶段 3：具体化代币输出（在块上并行）
30:fork= 1toMin 并行 do
31：Z(k)←U(k)+W(k)S(k)
在 .
32：H(k)←Q(k)S(k)
在 ▷ C×dv
33:P (k)←Q(k)A(k) ▷ C×(CB)
34：通过L(k)定义块因果掩码L(k)∈{0, 1}C×(CB)
i,(m−1)B+1:mB= I[m≤i]11×B（读后写）。
21

### 第 22 页

**P0022** 35: ^O(k)←H(k) +
（
P(k)⊙L(k))
Z(k).▷局部重整化输出
36:行重新缩放：O(k)
我，：←δ(k)
i ˆO(k)
i,:fori=1:C，并写入rowsa k:bk ofO。
37：结束
38:T T←{(xj,vj)}T
j=max(2,T−B+2)。
39：返回（O，ST，TT）。
块边界注释。在算法 4 中，阶段 1 中的重叠 setJ 是离线等价的
携带最后 B−1 个因果对跨越边界。仅传递块边界矩阵
状态S (k)
对于 FALCON-3，不相交的段之间的 in 并不精确；精确的延续还需要
边界 tailT（或等效的滚动窗口表示）。
关联块映射。要使扫描声明明确，请为 chunkk 定义
M(k) :=δ(k)
C
（
Idx+A(k)W(k))
,b (k) :=δ(k)
C A(k)U(k)。
那么边界更新就是仿射图
S(k+1)
=M(k)S(k)
在+b(k)中。
这些块映射关联地组成，
(M2,b 2)°(M1,b 1) = (M2M1,M 2b1 +b 2),
因此，第 2 阶段可以实现为算法 4 中所示的简单顺序循环，也可以实现为
对块映射进行关联扫描。
4.5 内积损失的小批量更新规则（FALCON-3A）
我们将相同的滑动窗口原理应用于内积目标。为了避免耦合
将缩放更新为标称窗口大小B（小批量缩放效果），我们使用窗口平均值
客观。 Fort≥2，letBt :=|It|≤B（并跳过边界更新 att = 1）。有窗的
损失是：
ℓip,(B)
t (St−1) =−1
转氨酶
Σ
j∈It
⟨
S⊤
t−1xj,vj
⟩
+ λt
2∥St−1∥2
F.(4.16)
定义窗口平均互协方差
́N(B)
t：= 1
转氨酶
Σ
j∈It
xjv⊤
j = 1
转氨酶
乙（乙）
t,N (B)
:=
Σ
j∈It
xjv⊤
j.
则梯度为∇Sℓip,(B)
t =−́N(B)
t+λtSt−1。
为什么朴素的（ηt，λt）参数化很差。如果将更新写为
St = (1−ηtλt)St−1+ηt N(B)
t ,(4.17)
那么递归由两个量控制：
ρt :=ηt 且 α t :=ηtλt。
22

### 第 23 页

**P0023** 只有ρt（写入系数）和αt（衰减分数）是动态的。参数化原始
因此 ridgeλt 是间接的，更糟糕的是，会忘记当前的写入幅度：增加
λt 既通过 αt 增强遗忘，又通过 ηt 的诱导减少来缩小当前写入。
对于内积目标，这种耦合是不必要的，因为特征能量项是
仅用于稳定写入幅度而不是匹配损失中的曲率项。
FALCON-3A 参数化。
́E(B)
t：= 1
转氨酶
Σ
j∈It
∥xj∥2
2、ρt:=βt
́E(B)
t+εη
，βt>0，εη>0，(4.18)
和
αt := (1−εγ)σ(～αt), γ t := 1−αtε[εγ,1), ε γ>0.(4.19)
对于学习到的内部步骤st≥2，该参数化产生αtε(0, 1−εγ)，因此γtε(εγ, 1)。
我们为硬边界/消融设置保留精确的无衰减情况αt = 0。特别是，对于
我们设置边界无操作(ρ1,α1) = (0,0)，所以γ1 = 1。F ALCON-3A 递归式是
St =γtSt−1+ρt N(B)
t.(4.20)
这完全将可塑性与遗忘解耦：ρt 确定当前写入幅度，而
αt 确定乘法进位。
与岭目标的关系。方程中的重现。 (4.20) 恰好是 a 导出的梯度步骤
窗式内积物镜，其脊为
λeff
t :=αt/ρt (ρt>0)。
每当一个步骤被迫成为严格的无操作时，我们设置(ρt,αt) = (0, 0)（因此 γt = 1）并且不
调用λeff
t。对于ρt>0，确实，
St =S t−1−ρt
（
-N(B)
t+λeff
t St−1
）
,
所以等式(4.20) 是 OGD，步长ρt 为
−⟨S, ́N(B)
t ⟩F + λeff
t
2∥S∥2
F。
因此，数学上相关的控制变量是衰减分数
αt =ρtλeff
t ,
不是单独参数化的原始脊。对于每个内部步骤，ρt > 0 且 αt∈[0, 1)，
等式。因此，（4.20）是这个导出的二次目标的下降步骤：它的平滑度是λeff
t 和
有效步长乘积为ρtλeff
t =αt < 2。当αt = 0 时，目标变为线性且
更新纯粹是附加的。
尺度属性。因为我们优化了窗口平均值，所以 ́N(B)
t 和 E(B)
t 是平均值
而不是总和，因此它们的典型比例不会随着标称窗口大小B而增长。如果写
特征本身是 RMS 归一化的（例如， 是恒等式或近似保持范数），则
23

### 第 24 页

**P0024** ́E(B)
对于任意B，t ≈dx；对于通用核映射 phi，正确的说法就是 ¯E(B)
t 轨道
实现的平均写入特征能量。因此，既不写系数ρt ́N(B)
t 也不是
衰减分数 αt 通过增加 B 被系统地放大。此外，在 RMS 归一化下
写入特征，ρt 尺度为 O(d−1
x )，这是加法的正确无量纲归一化
写路径。
当方程（4.18）中的分母消失时，我们再次采用约定ρt := 0（这可以
仅发生在简并边界情况（如果 εη= 0）；在强制无操作的情况下，我们还设置 αt := 0，所以
γt = 1。在实践中我们取εη>0。
远离边界（soBt = B）并且没有衰减（αt = 0，等效γt = 1），每个标记
出现在恰好 B 个连续窗口平均值中，权重为 1/B，因此在平稳情况下
常数 ρt = ρits 累积注入系数为 Σj+B−1
s=j ρ/B= ρ，独立于B。对于
B = 1 且 t≥2（因此 Bt = 1），N(B)
t =x 电视⊤
t 和方程（4.20）简化为非滑动内积
用解耦的衰减来写。在加性消融αt=0（或极限αt→0）中，更新
是纯粹的加法。相同的归一化仍然可以用作幅度稳定器，但它并不适用
对应于有界目标的下降。
这有效地将当前窗口的平均证据添加到每一步的状态中，
通过衰减因子来平衡。窗口化确保每个代币最多参与B个直接
写入交互，但其效果可以通过乘法进位持续超出那些 B 写入∏
rγr。因此，该机制产生一个可控的有效内存范围，而不是严格的局部内存范围。
支持；严格局部性需要显式截断（或硬零进位覆盖γt = 0，这
位于默认参数化γt≥εγ>0)之外。
流边界状态。正如 FALCON-3 中一样，矩阵状态单独不足以
继续 FALCON-3A 跨越段边界。精确延续需要最后一个 B−1 因果关系
对，或等效的 FIFO / 滚动缓冲区，其中活动窗口交叉协方差和
write-energy 可以准确更新。汇总统计数据，例如(́N(B)
t,E(B)
t ,Bt)单独不是
足以无限期延续，因为下一步必须删除最旧的贡献
正是如此。 4.6 节中的屏蔽注意力展开假设这种重叠是可用的。
4.6 小批量内积更新的并行（注意力）形式
我们证明了第 4.5 节中的循环滑动窗口更新在代数上等价于
具有结构化因果掩模的（非标准化）点积注意力矩阵。这个注意力矩阵
view 对于 GPU 并行训练很有用，是用于连接门控的标准展开技巧
屏蔽注意力公式的线性递归（例如，RetNet (Sun et al., 2023)、Lightning
注意力（Qin 等人，2024）、门控线性注意力（Yang 等人，2023）和 Mamba-2（Gu 和
道，2023；道和顾，2024））。
对于非钳位的阐述，令 γs := 1−αs 且 αs ∈[0, 1)，所以 γs ∈(0, 1]。展开
复发
Ss =γsSs−1+ρs ́N(B)
s
写后读的 timet 产生：
圣 =
( t ∏
r=1
γr
）
S0+
tΣ
s=1
( t ∏
r=s+1
γr
）
ρs N(B)
(4.21)
24

### 第 25 页

**P0025** 算法 5 FALCON-3A 的块并行屏蔽注意力（前向）
要求：查询featuresQ∈RL×dx（rowt为 phi(qt)⊤），写入featuresX∈RL×dx（rowt为x⊤）
t = phi(kt−1)⊤),
值V ∈RL×dv (rowt isv⊤
t ), 写出系数ρ∈RL
≥0，衰减分数αε[0, 1−εγ]L，窗口大小
B，块大小C(假设C|L)，衰减层εγ>0，初始状态S init∈Rdx×dv。
确保：输出O ∈RL×dv 其中行 t iso ⊤
t = phi(qt)⊤St （等效地，o t =S ⊤
t ψ(qt)) 在S t =
γtSt−1+ρt N(B)
t 与 ́N(B)
t = 1
转氨酶
Σ
j∈It xjv⊤
j 且 γt := 1−αt ∈[εγ,1]。
1：定义It={j|max(2,t−B+1)≤j≤t}对于所有t且setBt←|It|。
2：形成窗口权重时，使用B+
t ←max(1,Bt)使得 I[j∈It]/B+
t 总是明确定义的（并且
当B t = 0 时等于0)。
3：设置ρ1←0、α1←0、γ1←1（边界无操作）。对于 t≥2，计算 logγt←log1p(−αt) 并且
γt←exp(logγt)。
4：划分为M=L/C块，索引为[a k,bk] = [(k−1)C+1,kC]。
5：初始化输出缓冲器O←0L×dv。
6:▷阶段1：块内预计算（并行）
7:fork= 1toMin 并行 do
8:a←a k,b←bk,p←max(2,a−B+ 1)，且letJ :={p,p+1,...,b}(重叠长度|J|≤C+B−1)。
9:切片Q(k)←Qa:b,X(k)
ext←XJ,V(k)
ext←VJ，ρ (k)←ρa:b，和logγ (k)←(logγ)a:b。
10：计算块内的对数前缀衰减：u0←0和ui←ui−1+ logγ(k)
i 表示 i = 1,...,C（扫描）；
设δi←exp(ui)和g(k)←δC。
11:建立因果衰减矩阵D(k)∈RC×C和D(k)
i,s =I[s≤i]exp(ui−us)。
12：构建B带窗口算子A(k)∈RC×|J|（由j∈J索引的列）：A(k)
s,j = I[j∈
Ia+s−1]/B+
a+s−1。
13：局部掩码M(k)←D(k)Diag(ρ(k))A(k)∈RC×|J|。
14：块内输出O(k)
内←
（
(Q(k)X(k)
分机
⊤
)⊙M(k))
V(k)
分机。
15：边界传播的块偏差：m (k)
结束←M(k)
C,: 且 B (k)←X(k)
分机
⊤(
直径(m(k)
结束）V(k)
分机
）
。
16:Cacheg(k),B(k),δ(k):=(δ1,...,δC),andO(k)
内。
17：结束
18:▷阶段2：块间循环（连续块或扫描）
19:S (1)
在←Sinit
20：叉= 1toMdo
21：S（k）
出←g(k)S(k)
在+B (k)
22:S (k+1)
在 ←S(k)
出
23：结束
24:▷第3阶段：实现输出（并行）
25:fork= 1toMin 并行 do
26:H(k)←Q(k)S(k)
在 ▷ C×dv
27：O（k）
hist←Diag(δ(k))H (k)
28：O(k)←O(k)
历史+O(k)
内部
29：写O ak:bk←O(k)。
30：结束
31:returnO(连接{O(k)}M
k=1）。
在整篇论文中，我们使用边界约定 x1 = phi(k0) = 0 和 set( ρ1,α1) = (0, 0)
（因此 γ1 = 1），对于预填充，我们取 S0 =0；在默认情况下，总和实际上从
s=2。
在下一个潜在对齐下，定义写入特征xj := phi(kj−1)(sox j =k j−1when phi
25

### 第 26 页

**P0026** 是身份）。我们遵循之前的边界约定，即快速内存更新开始于
t = 2（等效地，x 1 =0，因此 t = 1 更新是无操作），因此滑动窗口索引满足
Is⊆{2,...,s}andMt,1 = 0. LetB s :=|Is|≤带召回
́N(B)
s = 1
乙烷
Σ
j∈Is
xjv⊤
j ,I s ={j|max(2,s−B+1)≤j≤s}。
然后：
圣 =
tΣ
s=2
tΣ
j=2
I[j∈Is]
（
ρs
乙烷
t∏
r=s+1
γr
）
xjv⊤
(4.22)
我们交换求和顺序，首先对 tokensj 求和。对于 j≥2，条件 j∈Is 为
等价于s−B+ 1≤j≤s，即j≤s≤j+B−1。将此与外界 ≤ t 结合起来，
给定标记的更新步骤的有效范围jisj≤s≤min(t,j+B−1)。因此：
圣 =
tΣ
j=2


min(t,j+B−1)Σ
s=j
ρs
乙烷
( t ∏
r=s+1
γr
)
xjv⊤
(4.23)
将标量掩码 Mt,j 定义为括号中的项（并且 Mt,j = 0，对于 j >t 且对于 j <2 下
我们的边界约定），位置的因果读数
ot =S⊤
t Φ(qt) =
tΣ
j=2
山,j
⟨
ψ(qt),xj
⟩
vj.(4.24)
等价地，堆叠查询特征Q∈RL×dx，rowt等于phi(qt)并写入特征
X ∈ RL × dx (行 j 是 x j )，并将值放入 V ∈ RL × dv 中，并用条目定义 M ∈ RL × L
Mt,j =M t,j 对于2≤j≤t（且0否则），序列级读数为O= (QX⊤⊙M)V。
这是一种带有玛瑙依赖（输入条件）因果掩码的掩码注意力形式，由
滑动窗口小批量和乘法衰减：每个系数 Mt,j 最多是 B 的总和
衰减写入系数 ρs/Bs，并且对于 allj≤t 通常不为零。重要的是，Mt,j需要
lagt−j 中不是单调的：因为 tokenj 被注入到 N(B) 中
s 对于 s =j,...,j +B−1,
最近 B 位置内的权重可以增加 asj 向后移动（较旧的令牌是
重新添加更多次），而过去更远的标记在γr∈(0, 1)时呈几何衰减。在
静止情况ρs =ρ且γs =γε(0,1),
Mt,j = ρ
乙
t−jΣ
u=max(0,t−j−B+1)
γu,
这使得移动和指数尾结构变得明确。以上所有表达式均遵循
我们的全纸读后写约定：positiont 处的读数使用更新后的 stateSt 和
用于预测tokent+1。
掩码结构。掩码Mt,j表示token对j的累积有效学习率
按时间：
Mt,j =
min(t,j+B−1)Σ
s=j
ρs
乙烷
t∏
r=s+1
(1−αr),(4.25)
26

### 第 27 页

**P0027** 该掩码干净地将窗口平均累加与全局乘法进位分开
γt = 1−αt，便于GPU实现。
对数空间累积衰减。直接形成 ∏t
r=s+1γr 可以在长序列上下溢，
特别是在精度降低的情况下。定义对数累积衰减
δ0 := 0, δ t :=δt−1+ logγt (t≥1),
使得 ∏t
r=s+1γr = exp(zt−zs) 且
Mt,j =
min(t,j+B−1)Σ
s=j
ρs
乙烷
exp(zt−zs).(4.26)
为了在 αt 很小时提高精度，计算 logγt = log(1−αt)aslog1p(−αt)（在 fp32 中）。
块局部重整化。即使使用日志表示，也显式物化全局
zt 然后以非常大的滞后形成 exp(zt−z) 可能会导致精度降低（并且是
对于分块内核来说是不必要的）。因此，本文中的所有块并行算法都重置了
在块边界处将日志前缀设置为 0，仅使用块内差异 ui−us 且 u0 = 0，并且
通过块出口衰减传播边界状态g(k) = ∏
t∈块kγt。这正是当地的
算法 5 使用的对数衰减技巧（及其附录 B.1 中的向后传递）。
块并行评估。为了实现，将掩模暴露为组合是有帮助的
(i) 因果衰减核和 (ii) aB 带移动平均窗口算子。定义
Dt,s :=I[s≤t]
t∏
r=s+1
γr,B+
s := max(1,Bs), A s,j := I[j∈Is]
B+s
,M=DDiag(ρ)A,
soMt,j = Σ
sDt,sρsAs,j 恢复方程。 （4.26）。在边界约定下（ I1 =∅且ρ1 = 0），
s = 1 行 A 全部为零，对于所有 ≥ 2，我们有 B+
s =Bs，所以这与As,j = I[j∈Is]/Bs一致
同时避免 s = 1 处的 a0/0 定义。这产生了一个三相分块算法：per-chunk
预计算（并行）、块间边界传播（短循环/扫描）和最终
输出物化（并行）。
向后传递。算法 5 的数值稳定向后传递，通过微分
结构化掩码和块局部对数衰减重整化在附录 B.1 中给出
（算法 6）。
与测试时训练和泰坦式记忆目标的关系。测试时训练 (TTT)
通过在内部、自我监督的目标上采取梯度步骤来更新推理时的模型参数子集，并且最近的工作将这种机制形式化为特征中的测试时回归
空间（Sun 等，2024；Wang 等，2025）。我们的快速权重更新是受约束的单步更新
该范式的实例：循环状态是适应参数，方程（3.1）是内部
客观。相对于通用 TTT，我们通过仅在之后更新来强制执行严格的自回归因果关系
vt 被揭示，并通过将其写在预测时可用的前缀特征下，xt = phi(kt−1)。
Titans 和 ATLAS 同样将循环状态视为具有在线优化的快速内存
流上的内部目标（Behrouz et al., 2024, 2025a），我们的贡献是使
键值对齐显式并导出归一化一阶规则和滑动窗口变体
与 SSD 风格的块并行训练兼容。
27

### 第 28 页

**P0028** 5 实验
我们的主要实证评估是大约 124M-130M 参数的语言建模。我们
使用匹配的 50B 代币预算训练 FineWeb-Edu 上的所有模型，并评估保留的模型
困惑度和下游任务的准确性。然后我们使用两个支持诊断： 可变长度
多位加法，隔离因果存储和长度外推，以及长上下文
检索，探索干扰下的记忆。除非表行另有说明，否则快速权重
模型使用第 4.1 节的缩放公式，并且下一个潜在对齐是通过以下方式实现的
使用 ZeroBOSwrite 功能将写入密钥流移动一个位置，因此第一次写入是
无操作。对于 FALCON-2A，行标签 ctxβ 和 ctx η 是运行标识符，指示学习的是否
控制被解释为NLMS增益βt或归一化后写入系数ηt；的
4.3 节中的理论以增益参数化形式写成方程（4.8）。血统讨论
直接适用于增益参数化形式； direct-etat 运行应被理解为经验
除非对 λt > 0 的所有步骤单独强制执行 ηtλt< 2。对于 FALCON-3A，
实际递归总是使用第4.5节的解耦(ρt,αt)参数化；标签如
ctxη-ctxλ 是旧版运行名称，应仅读取为上下文相关的可塑性/衰减标签，
不是字面量 (ηt,λt) 状态变量。 QK-RMSNorm 和 QK-ℓ2 表示查询/密钥标准化
在快速重量块内使用。
5.1 语言建模实验
我们训练 124M-130M 参数模型，进行 100,000 个优化步骤，序列长度为 1,024
全局批量大小为 480，总预算约为 49.2B 代币（约 50B）。的
Transformer 基线使用带有 RoPE 和 SwiGLU 的 LLaMA 风格架构。经常性基线
包括 RetNet/LightningAttn、Mamba-2、DeltaNet 和门控 DeltaNet。完成的猎鹰
这种规模的语言建模运行是 FALCON-2A 变体。除非明确消融，否则快速权重模型在注意力投影上使用 QK-RMSNorm 和轻量级短卷积。
对于这些运行，所有模型都使用 AdamW 在 bfloat16 中进行训练，绑定输入/输出嵌入，
Pre-Norm RMSNorm，无偏差项，无 dropout。我们使用μP式的宽度缩放，基础学习
速率 10−3，余弦衰减和 2,000 个预热步骤，(β1,β2) = (0.9, 0.95)，权重衰减 0.1，并且
梯度裁剪为 1.0。所有运行均在单个 4-GPU 节点（H100 或 H200）上执行。我们报道
教师强制的困惑和零样本/单样本下游精度。
在验证集困惑度上，如表 1 所示，FALCON 有竞争力，但不均匀
强于最佳基线。我们的变体包括 FALCON-2A.3（QK-RMSNorm、ctxη-ctxλ）
给出了最好的困惑度，在 FineWeb-Edu 验证中达到 17.64，而 Gated DeltaNet 仍然如此
此处比较的模型中整体最强。表2显示，相同的next-latent
更新也相当好地转移到下游评估：FALCON-2A.2 取得了最佳成绩
在此比较中，其零射击平均值，其单射击平均值仍与 Mamba-2 具有竞争力
和变压器。在 FALCON 系列中，QK-RMSNorm 改善了相对于
QK-ℓ2 归一化，而 ctxη 相对于相应的 ctxβ 提高了平均下游精度
变体。因此，主要的收获并不是在这种规模上取得一致的胜利，而是我们的自回归
对齐的、标准化的快速权重更新在现实的预训练下保持语言模型的质量
预算。
28

### 第 29 页

**P0029** 表 1 在 FineWeb-Edu 上针对 50B 代币预算训练的具有 124M-130M 参数的模型比较。
越低越好。
模型困惑↓
维基百科。左键。精细教育。
124M-130M参数
(124M) 变压器（带绳索）33.25 47.43 17.38
(130M) RetNet/LightningAttn36.86 65.16 18.79
(130M) 曼巴-234.53 48.74 17.70
(130M) DeltaNet34.19 52.84 17.84
(130M) 门控 DeltaNet30.99 46.70 17.32
我们的
(130M)FALCON-2A.1 (QK-ℓ2-范数, ctxβ-ctxλ) 34.41 47.93 17.70
(130M)FALCON-2A.2 (QK-ℓ2-范数, ctxη-ctxλ) 34.20 51.01 17.70
(130M)FALCON-2A.3 (QK-RMSNorm, ctxη-ctxλ)34.02 49.84 17.64
表 2 语言模型转移到下游任务。零样本和单样本精度。精度使用
ACC；标记为*use acc_n 的任务如 lm-evaluation-harness 中所示。平均。是未加权平均值
8 项任务。
模型精度↑
PIQA Hella。* 酒诺。 ARC-e ARC-c* OBQA* 社交 IQA SciQ 平均。
零射击（0-shot）
124M-130M参数
(124M) 变压器（带绳索）65.67 37.54 51.70 52.36 27.65 31.60 38.84 79.90 48.16
(130M) RetNet/LightningAttn64.91 35.36 49.64 57.62 26.28 32.20 37.97 80.40 48.05
(130M) 曼巴-266.32 36.89 50.75 58.16 26.62 32.60 38.38 80.70 48.80
(130M) DeltaNet66.38 37.15 52.33 57.37 26.79 34.00 39.00 78.00 48.88
(130M) 门控 DeltaNet65.51 37.75 49.72 58.88 27.90 31.60 38.28 80.60 48.78
我们的
(130M)FALCON-2A.1 (QK-ℓ2-范数, ctxβ-ctxλ)66.05 37.27 50.67 57.62 27.65 31.60 38.95 81.1048.86
(130M)FALCON-2A.2 (QK-ℓ2-范数, ctxη-ctxλ)67.03 37.29 52.33 57.37 25.94 33.20 38.84 82.4049.30
(130M)FALCON-2A.3 (QK-RMSNorm, ctxη-ctxλ)66.10 37.55 50.12 59.01 26.79 32.00 37.82 82.2048.95
一发（一发）
(124M) 变压器（带绳索）66.43 37.55 50.28 59.64 29.01 30.00 39.82 84.60 49.67
(130M) RetNet/LightningAttn65.13 35.19 49.64 56.78 25.85 28.80 36.44 81.50 47.42
(130M) 曼巴-266.70 36.61 51.07 58.63 26.96 32.40 37.97 82.90 49.16
(130M) DeltaNet66.27 36.67 50.36 57.70 27.39 32.00 37.72 79.90 48.50
(130M) 门控 DeltaNet65.40 37.81 51.30 58.29 26.88 30.00 37.26 81.60 48.57
我们的
(130M)FALCON-2A.1 (QK-ℓ2-范数, ctxβ-ctxλ)66.81 37.41 50.75 57.53 27.99 32.40 37.92 81.8049.08
(130M)FALCON-2A.2 (QK-ℓ2-范数, ctxη-ctxλ)66.38 36.97 52.96 57.32 27.82 31.40 37.56 83.2049.20
(130M)FALCON-2A.3 (QK-RMSNorm, ctxη-ctxλ)65.78 37.22 49.72 58.88 27.73 31.40 37.97 82.4048.89
5.2 变长算术加法任务
我们使用可变长度的多位加法作为因果存储的受控压力测试
遵循 Kaiser 和 Sutskever (2015) 的推断。每个样本均呈现一位数字提示
29

### 第 30 页

**P0030** sin :="数字 n(a)+数字 n(b)=",
并要求模型生成反转的 (n+1) 位和
南：=“转
（
数字n+1(a+b)
）
“，
以便首先产生最低有效数字。我们根据均匀采样的宽度进行训练
[1, 32]并仅优化目标后缀上的屏蔽下一个标记对数似然。评价用途
教师强制和序列级精确匹配精度；在精确匹配下，这相当于
对目标后缀的贪婪解码成功。对于长度外推，我们保持模型固定并且
扫描评估宽度N∈{33,...,48}。
表 3 报告了分布内验证准确性和分布外精确匹配
平均准确度超过 33-48 位。 FALCON-3A.3实现了最佳的外推性能，
平均准确度为 87.2，其次是 FALCON-2A.3，平均准确度为 85.9；都优于线性注意力并且
Transformer 执行此任务。我们认为这个实验是支持证据而不是
论文的主要结果：它隔离了循环状态的记忆写入行为并显示
移位的快速内存更新大大提高了存储时的长度泛化能力
进位传播占主导地位。
表 3 教师强制的可变数字加法长度概括。越高越好。
模型最佳步骤值。附件平均按照。 ACC@d33/d48
变压器（带 RoPE）2000 100.0 65.897.0/49.0
线性衰减 (WD)2000 99.7 82.999.0/63.0
线性衰减 (ctxλ)2000 100.0 75.2100.0/51.0
我们的
FALCON-3A.3（QK-RMSNorm，ctxη-ctxλ）2000 99.9 87.2 100.0/69.0
FALCON-2A.3（QK-RMSNorm，ctxη-ctxλ） 1900 99.8 85.9 100.0/69.0
FALCON-2A.2 (QK-ℓ2-范数, ctxη-ctxλ) 2000 100.0 85.2 100.0/63.0
FALCON-3A.1（QK-ℓ2-范数，ctxβ-ctxλ） 1900 100.0 83.7 100.0/63.0
FALCON-2A.1（QK-ℓ2-范数，ctxβ-ctxλ） 1900 100.0 80.6 100.0/59.0
FALCON-3A.2（QK-ℓ2-范数，ctxη-ctxλ）2000 100.0 78.8 99.0/56.0
5.3 长上下文检索任务
我们还评估长上下文检索和上下文回忆任务的语言模型检查点，
包括 RULER 式单针和多针检索、变量跟踪和常用词
提取。这些任务旨在作为内存诊断而不是主要基准，因为
他们比其他人更直接地强调在干扰因素下的检索和长时间跨度的简单构图
困惑确实如此。
表 4 报告了当前已完成的运行。由于有几个条目仍悬而未决，我们这样做
请勿使用此表来支持论文的主要主张或模型选择。已完成的行
尽管如此，还是要说明评估制度并表明这种规模的长情境行为是
架构和任务之间高度不平衡；我们推迟完整的比较，直到剩余的运行
完成了。
30

### 第 31 页

**P0031** 表4 长上下文检索结果。124M-130M参数训练的模型比较
FineWeb-Edu 上的 50B 代币预算。所有条目均为长上下文检索准确率（%，越高越好）
根据匹配的评估协议。显示为 TBD 的条目表示尚未完成的运行
写作时，并没有在文本中讨论。
模型 NIAH 风格↑上下文回忆/构图↑
S-NIAH-1024 S-NIAH-2048 MultiQuery-NIAH VarTrack CommonWords
124M-130M参数
(124M) 变压器（带绳索）100.00 7.81 1.95 0.31 8.75
(130M) RetNet/LightningAttn待定 待定 待定 待定
(130M) 曼巴-243.75TBD12.11 10.31 0.16
(130M) DeltaNet85.93 12.50 13.28 14.69 2.97
(130M) 门控 DeltaNet71.88 9.38 3.13 18.13 0.00
我们的
(130M)FALCON-2A.1（QK-ℓ2-范数，ctxβ-ctxλ）待定 待定 待定 待定
(130M)FALCON-2A.2（QK-ℓ2-范数，ctxη-ctxλ）待定 待定 待定 待定
(130M)FALCON-2A.3 (QK-RMSNorm, ctxη-ctxλ) 待定 待定 待定 待定
6 相关工作
高效序列建模和状态空间对偶性。标准变压器（Vaswani 等人，
2017）由于注意力的具体化而产生quadraticO(N 2)计算和内存成本
矩阵。为了解决这个问题，线性注意力（Katharopoulos et al., 2020）通过内核特征图重新排序矩阵乘法，有效地将上下文视为循环累积
外部产品。其他次二次注意力替换包括随机特征近似
到softmax注意力，例如Performer（Choromanski et al., 2020）、长卷积算子
例如 Hyena (Poli et al., 2023) 和保留型循环注意力 (Sun et al., 2023) 或
RNN-Transformer 混合体（Peng 等人，2023）。结构化状态空间的并行开发
模型 (SSM)，例如 S4 (Gu et al., 2021) 和 Mamba (Gu and Dao, 2023)，利用离散化
连续时间动态来实现类似的线性缩放。最近，Mamba-2（道和古，
2024）将这些范式统一在结构化状态空间二元性（SSD）下，证明选择性
SSM 在算法上与具有半可分离掩码的线性注意力对偶。而 Mamba-2 和
相关的 SSD 风格模型侧重于硬件利用率（通过块并行扫描）和表现力
离散化，它们很大程度上保留了附加或门控累积作为状态写入规则。相反，我们的工作重新审视引起状态更新的目标。我们通过自回归推导
对齐的、NLMS 稳定的回归更新和保持兼容的滑动窗口变体
具有 SSD 风格的块并行训练。
快速权重和 Delta 网络。快速权重的概念（Schmidhuber，1992）指出
神经网络可以维护一个由立即更新的高容量短期记忆矩阵
输入流。施拉格等人。 (2021) 将其形式化为线性变压器秘密地快速重量
程序员提出了 Delta Network，它通过梯度步骤更新状态
预测误差而不是赫布加法。杨等人。 (2024a) 通过 Gated Delta 扩展了这一点
网络，引入 Mamba 式门控以提高稳定性。然而，这些先前的方法
通常将更新规则视为架构选择而不是优化问题。
31

### 第 32 页

**P0032** 自适应过滤和在线回归。快速权重模型中使用的增量规则更新是
与经典自适应滤波密切相关，其中 LMS delta 规则及其归一化变体
（NLMS）是用于规模鲁棒在线回归的标准算法，以及二阶方法，例如
因为递归最小二乘法 (RLS) 以更高的成本提供精确的在线岭更新（Sayed，2011）。
我们基于 NLMS 的 FALCON-2 更新可以被视为导入这些稳定性/标准化
将原则纳入严格因果关系下的快速重量注意力，同时保持与现代的兼容
线性递归的并行实现（Yang 等人，2024b；Dao 和 Gu，2024；Cirone 和
萨尔维，2025）。
上下文学习作为隐式优化。最近的理论工作解释了前向传递
Transformers 在上下文示例中执行隐式（预处理）梯度下降
（Von Oswald 等人，2023；Ahn 等人，2023；Cheng 等人，2023）。冯·奥斯瓦尔德等人。 （2025）明确
通过提出解决前向最小二乘问题的 Mesa-Layers 来实现这一点
通过。 MesaNet 通过求解（或近似求解，取决于
停止标准）通过共轭梯度的顺序线性系统，设计明确针对
以块并行方式，以增加推理计算为代价。与网络类比
分层即分解，我们的方法符合这种层作为优化器的哲学（Chiang
et al., 2007），但将解决方案限制为严格的在线更新。我们形式化状态空间
模型作为在线岭回归器预测下一个嵌入。这种观点使我们能够
将滑动窗口注意力机制与循环状态更新统一起来，从而产生滑动窗口
状态机制：通过小批量进行有限窗口遗忘的数学基础方法
SGD（Behrouz 等人，2025a），弥合了局部注意力和全局复发之间的差距。
上下文工程和内存管理。设计更好的注意力的补充
以及硬件高效的循环快速存储器（Guo 等人，2025；Zhang 等人，2025b），
一系列工作研究情境工程：管理和转变呈现给他人的情境
模型。这包括 KV 缓存压缩、量化或驱逐方案，以减少
长上下文开销，同时保留显着信息（Liu et al., 2024c；Xiao et al., 2024；
张等人，2025a)。其他方法通过多时间尺度记忆来看待情境学习
将短期激活与突触/可塑性快速权重相结合的系统（Schlag et al., 2021；
Behrouz 等人，2025b)。
7 结论
在本文中，我们将自回归序列模型重新定义为显式的下一个潜在预测
问题。根据始终使用的先写后读约定，常见的快速权重循环
暂时移动一步；我们通过使用一个框架来明确这一转变，其中
状态映射skt−1tov t。在此镜头的基础上，我们推出了F ALCON-2（NLMS-归一化
增量更新），F ALCON-3（通过单个小批量梯度步骤的滑动窗口回归），以及
内积对应物F ALCON-2A/F ALCON-3A。在我们的受控添加任务中
∼130M FineWeb-Edu 语言建模实验，这些更新具有强大的竞争力
线性循环基线，同时使用显式自回归对齐的下一个潜在写入约定。
更广泛地说，我们的框架将 SSM 式序列模型作为持续学习系统
明确的快速记忆目标，提供稳定性与可塑性权衡的原则性处理
通过习得的可塑性、遗忘和有限的排练。
32

### 第 33 页

**P0033** 参考文献
乔什·阿希亚姆、史蒂文·阿德勒、桑迪尼·阿加瓦尔、拉马·艾哈迈德、伊尔格·阿卡亚、弗洛伦西亚·莱奥尼·阿莱曼、
迪奥戈·阿尔梅达、扬科·阿尔滕施密特、萨姆·奥尔特曼、Shyamal Anadkat 等。 GPT-4技术
report.arXiv 预印本 arXiv:2303.08774, 2023。
Kwangjun Ahn、Xiang Cheng、Hadi Daneshmand 和 Suvrit Sra。变压器学习实施
用于上下文学习的预条件梯度下降。神经信息处理的进展
系统，36：45614–45650，2023。
Jimmy Ba、Geoffrey E Hinton、Volodymyr Mnih、Joel Z Leibo 和 Catalin Ionescu。使用快
关注最近的权重。神经信息处理系统的进展，2016 年 29 月。
阿里·贝鲁兹、钟培林和瓦哈卜·米罗克尼。泰坦：在考试时学习记忆.arXiv
预印本 arXiv:2501.00663, 2024。
Ali Behrouz、Zeman Li、Praneeth Kacham、Majid Daliri、邓远、钟培林、Meisam
拉扎维亚因和瓦哈卜·米罗克尼。 Atlas：学习以最佳方式记住测试时的上下文
time.arXiv 预印本 arXiv:2505.23735, 2025a。
Ali Behrouz、Meisam Razaviyayn、钟培林和 Vahab Mirrokni。嵌套学习：错觉
深度学习架构。arXiv 预印本 arXiv:2512.24695, 2025b。
克里斯蒂安·比绍夫和查尔斯·范·洛恩。 Housener 矩阵乘积的 wy 表示。
SIAM 科学与统计计算杂志，8(1):s2–s13，1987 年。
程翔、陈雨馨、Suvrit Sra。 Transformer 实现函数梯度下降
在 context.arXiv 预印本 arXiv:2312.06528, 2023 中学习非线性函数。
蒋孟、史蒂文·H·洛、罗伯特·卡尔德班克和约翰·C·多伊尔。分层作为优化
分解：网络架构的数学理论。IEEE 会议录，95(1)：
255-312，2007。
Krzysztof Choromanski、Valerii Likhosherstov、David Dohan、宋星佑、Andreea Gane、Tamas
萨洛斯、彼得·霍金斯、贾里德·戴维斯、阿弗罗兹·莫希丁、卢卡斯·凯泽等。重新思考注意力
与 Performers.arXiv 预印本 arXiv:2009.14794, 2020。
尼古拉·穆卡·西罗内和克里斯托弗·萨尔维。 Parallelflow：通过流并行化线性变压器
Discretization.arXiv 预印本 arXiv:2504.00492, 2025。
Tri Dao 和 Albert Gu。 Transformers 是 ssms：通用模型和高效算法
结构化状态空间对偶性.arXiv 预印本 arXiv:2405.21060, 2024。
Daniel Y Fu、Tri Dao、Khaled K Saab、Armin W Thomas、Atri Rudra 和 Christopher Ré。
饥饿的河马：使用状态空间模型进行语言建模.arXiv 预印本
arXiv：2212.14052，2022。
Riccardo Grazzi、Julien Siems、Arber Zela、Jörg KH Franke、Frank Hutter 和 Massimiliano
庞蒂尔。通过负特征值解锁线性RNN中的状态跟踪.arXiv预印本
arXiv：2411.12537，2024。
33

### 第 34 页

**P0034** Albert Gu 和 Tri Dao。 Mamba：具有选择性状态空间的线性时间序列建模.arXiv
预印本 arXiv:2312.00752, 2023。
艾伯特·顾、卡兰·戈尔和克里斯托弗·雷。使用结构化的方法对长序列进行高效建模
状态空间.arXiv 预印本 arXiv:2111.00396, 2021。
韩国、杨松林、Tarushii Goel、Eric P Xing、Tri Dao 和 Yoon Kim。对数线性注意力。
arXiv 预印本 arXiv:2506.04761, 2025。
杰弗里·E·辛顿 (Geoffrey E Hinton) 和大卫·C·普劳特 (David C Plaut)。使用快速的重量来模糊旧的记忆。诉讼中
认知科学学会第九届年会，第 177-186 页，1987 年。
武卡斯·凯撒和伊利亚·苏茨克维尔。神经 GPU 学习算法.arXiv 预印本 arXiv:1511.08228,
2015年。
安杰洛斯·卡塔罗普洛斯、阿普尔夫·维亚斯、尼古拉斯·帕帕斯和弗朗索瓦·弗勒雷。变形金刚是
rnns：具有线性注意力的快速自回归变压器。在国际会议上
机器学习，第 5156-5165 页。 PMLR，2020。
刘波、王锐、吴乐萌、冯一浩、彼得·斯通和刘强。 Longhorn：状态空间
模型摊销在线学习者。arXiv 预印本 arXiv:2407.14207, 2024a。
刘子成、李思源、王丽、王泽东、刘云帆和李斯坦。短长卷积有助于硬件高效的线性注意力集中于长序列。arXiv 预印本
arXiv：2406.08128，2024b。
刘自瑞、袁佳怡、金红叶、钟绍辰、徐兆灼、Vladimir Braverman、Beidi
陈、夏虎. Kivi：kv 缓存的免调整非对称 2 位量化.arXiv 预印本
arXiv：2402.02750，2024c。
彭波、埃里克·阿尔凯德、昆汀·安东尼、阿隆·阿尔巴拉克、塞缪尔·阿卡迪尼奥、斯特拉·比德曼、欢奇
曹、程鑫、Michael Chung、Matteo Grella 等。 Rwkv：为变压器重新发明 rnn
era.arXiv 预印本 arXiv:2305.13048, 2023。
迈克尔·波利、斯特凡诺·马萨罗利、Eric Nguyen、Daniel Y Fu、Tri Dao、Stephen Baccus、Yoshua
本吉奥、斯特凡诺·埃尔蒙和克里斯托弗·雷。鬣狗层次结构：走向更大的卷积
语言模型。国际机器学习会议，第 28043-28078 页。 PMLR,
2023 年。
秦臻、孙伟高、李东、沉旭阳、孙伟轩、钟怡然。闪电关注——
2：在大型语言模型中处理无限序列长度的免费午餐。arXiv 预印本
arXiv：2401.04658，2024。
Ali H Sayed。自适应过滤器。约翰·威利父子公司，2011。
伊曼诺·施拉格、入江一树和于尔根·施米德胡贝尔。线性变压器秘密快速重量
程序员。国际机器学习会议，第 9355-9366 页。 PMLR，2021。
于尔根·施米德胡贝尔。学习控制快速重量记忆：动态循环的替代方案
神经计算，4(1)：131–139，1992。
34

### 第 35 页

**P0035** 孙宇、李新浩、Karan Dalal、徐嘉瑞、Arjun Vikram、张庚瀚、Yann Dubois、Xinlei
陈，王小龙，Sanmi Koyejo，等。学习（在测试时学习）：具有表达性的 Rnn
隐藏状态.arXiv 预印本 arXiv:2407.04620, 2024。
孙玉涛、董立、黄少涵、马树明、夏玉清、薛继龙、王建勇、
福如伟。保持网络：大型语言模型转换器的继承者。arXiv
预印本 arXiv:2307.08621, 2023。
Gemini 团队、Rohan Anil、Sebastian Borgeaud、Jean-Baptiste Alayrac、Jiahui Yu、Radu Soricut、
Johan Schalkwyk、Andrew M Dai、Anja Hauth、Katie Millican 等。双子座：家庭富裕
强大的多模式模型。arXiv 预印本 arXiv:2312.11805, 2023。
Kimi Team, 张宇, 林宗宇, 姚兴成, 胡嘉熙, 孟凡庆, 刘成银, 辛
门，杨松林，李志远，等。 Kimi Linear：一种富有表现力、高效的注意力架构。
arXiv 预印本 arXiv:2510.26692, 2025。
雨果·图夫龙、蒂博·拉夫里尔、戈蒂埃·伊扎卡尔、泽维尔·马丁内特、玛丽-安·拉肖、蒂莫西
拉克鲁瓦、巴蒂斯特·罗齐埃、纳曼·戈亚尔、埃里克·汉布罗、费萨尔·阿扎尔等。骆驼：开放和
高效的基础语言模型。arXiv 预印本 arXiv:2302.13971, 2023。
阿什什·瓦斯瓦尼 (Ashish Vaswani)、诺姆·沙泽尔 (Noam Shazeer)、尼基·帕尔玛 (Niki Parmar)、雅各布·乌什科雷特 (Jakob Uszkoreit)、Llion Jones、艾丹·N·戈麦斯 (Aidan N Gomez)、卢卡斯 (Łukasz)
凯撒和伊利亚·波洛苏欣。你所需要的就是注意力。神经信息处理的进展
系统，2017 年 30 月。
约翰内斯·冯·奥斯瓦尔德、艾温德·尼克拉森、埃托·兰达佐、若昂·萨克拉门托、亚历山大·莫德文采夫、安德烈·日莫吉诺夫和马克斯·弗拉迪米罗夫。 Transformer 通过梯度在上下文中学习
血统。国际机器学习会议，第 35151-35174 页。 PMLR，2023。
约翰内斯·冯·奥斯瓦尔德、尼诺·谢勒、小林诚人、卢卡·维萨里、杨松林、马克西米利安
施莱格尔、Kaitlin Maile、Yanick Schimpf、Oliver Sieberling、Alexander Meulemans 等。梅萨内特：
通过局部最优测试时间训练进行序列建模。arXiv 预印本 arXiv:2506.05233, 2025。
柯亚历山大·王、施嘉欣和艾米丽·B·福克斯。测试时回归：统一框架
使用联想记忆设计序列模型。arXiv 预印本 arXiv:2501.12352, 2025。
肖光轩、唐家明、左经纬、郭俊贤、商阳、唐浩天、付耀、
还有宋瀚。 Duoattention：通过检索和流式传输进行高效的长上下文 llm 推理
Heads.arXiv 预印本 arXiv:2410.10819, 2024。
杨松林、王柏林、沉义康、Rameswar Panda 和 Yoon Kim。门控线性注意力
具有硬件高效训练的变压器。arXiv 预印本 arXiv:2312.06635, 2023。
杨松林、扬·考茨和阿里·哈塔米扎德。门控 Delta 网络：改进 mamba2
Delta Rule.arXiv 预印本 arXiv:2412.06464, 2024a。
杨松林、王柏林、张宇、沉义康、Yoon Kim。并行化线性变压器
序列长度的增量规则。arXiv 预印本 arXiv:2406.06484, 2024b。
张一凡、刘一峰、袁慧卓、秦臻、袁阳、谷泉泉、安德鲁
姚期智.张量积注意力就是您所需要的。arXiv 预印本 arXiv:2501.06425, 2025a。
35

### 第 36 页

**P0036** 张一凡、秦臻、顾泉泉。高阶线性注意力.arXiv 预印本
arXiv：2510.27258，2025b。
36

### 第 37 页

**P0037** 附录
通过递归最小二乘法的精确在线岭 38
B 有关内积损失小批量更新规则 (FALCON-3A) 的更多信息 38
B.1 块并行 FALCON-3A 的向后传递。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 38
C 在序列长度维度 40 上并行化 Delta 网络
C.1 仿射表示和 WY 形式。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 41
C.2 分块并行算法。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 41
C.3 并行 DeltaNet 训练（向后）。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 43
D FALCON-2 的并行实现 44
D.1 通道无关矢量化形式。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 45
D.2 算法。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 45
E FALCON-2-Lite：高效共享动态 48
E.1 表述：标量学习率与向量学习率。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 49
E.2 通过共享 WY 分解并行实现。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 50
E.3 算法。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 50
E.4 复杂性分析。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 52
F 通过 ParallelFlow 高效训练 FALCON-3 53
F.1 背景：矩阵值 CDE 和低阶驱动程序。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 53
F.2 将 FALCON-3 映射到 ParallelFlow 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 55
37

### 第 38 页

**P0038** 通过递归最小二乘法精确在线岭
附录符号。我们重用主文本回归符号：x t 表示写入特征
（通常为下一个潜在对齐下的 phi(kt−1)），y t 为目标（通常为 vt），St 为快速权重
状态。残差rt =y t−S⊤
t−1xt 与正文中的 r t 匹配。
递归最小二乘法（RLS）。RLS在线实现精确的累积岭回归解
使用矩阵求逆引理。这里我们处理常数脊的情况λ>0，这不是精确的
本文其他地方使用的时变λt递归的封闭式解决方案。让
Pt−1:=
（
λI+Σt−1
s=1 xsx⊤
s
)−1,P 0 :=λ−1I(λ>0)。
定义增益和协方差更新
gt = Pt−1xt
1 +x⊤
t Pt−1xt
,P t =P t−1−gtx⊤
t Pt−1.(A.1)
那么参数更新就是
rt :=y t−S⊤
t−1xt,S t =S t−1+gt r⊤
t .(A.2)
这些递归成本O(d2
x +dxdv)每一步，其中 x := dim(xt)，并且满足 St 是精确的
处理样本后的岭回归解，即累积岭的最小者
{(xs, ys)}t 上的目标
s=1 在前面所述的情况下。这种形式的精确在线求解器要困难得多
比一阶递归要并行化，这就是 MesaNet (von Oswald et al., 2025) 使用
近似块并行方法。
B 更多关于内积损失的小批量更新规则 (FALCON-
3A）
B.1 块并行 FALCON-3A 的向后传递
算法 6 FALCON-3A 的块并行屏蔽注意力（向后）
要求：上游梯度 ¯O∈RL×dv 和算法 5 中缓存的前向张量。特别是，对于每个
chunkk∈{1,...,M}（带有索引[ak,bk]和重叠setJk），cache:Q (k) =Q ak:bk,X (k)
ext=XJk,V(k)
分机 =
VJk, ρ(k) = ρak:bk, α(k) = αak:bk, logγ (k) = (logγ )ak:bk, 局部前缀esu(k)
0:C 和 δ(k) = (δ1,...,δC)，衰减
kernelD (k), 窗口算子A(k), maskM (k), 块退出衰减g(k) =δC, 最后一行maskm(k)
结束=M(k)
C、：、
和边界态S(k)
在 .
确保：梯度 ̵Q ∈ RL × dx、 ̅ X ́ RL × dx、 ̅ V ́ RL × dv、 ̅ρ ́ RL、 ̅ α ́ RL 和 ̅ Sinit ̅ Rdx × dv。
1：将 ̷O 重塑为块 ̅O(k) = ̅Oak:bk。
2：将所有输出梯度初始化为零。初始化每块缓冲区：S(k)
输入，输出←0，δ(k)←0，M(k)←0，
̵Q(k)←0,̵X(k)
ext←0,V(k)
外部←0。
3:▷阶段3⊤：通过输出物化进行反向传播（并行）
4:fork= 1toMin 并行 do
5： 奥希斯特←O(k)，奥内特拉←O(k)。
38

### 第 39 页

**P0039** 6:▷历史术语：O hist = Diag(δ)(QSin)
7:H←Q(k)S(k)
在 ▷ C×dv
8: H←Diag(δ(k))Ohist
9: δ(k) += row_dot( Ohist,H)
10: Q(k) += H(S (k)
在）⊤
11：S(k)
输入，输出 += (Q(k))⊤́H
12:▷内项:O内=((QX⊤
ext)⊙M)Vext
13:P←Q(k)(X(k)
ext)⊤,W←P⊙M (k)
14：W←Ointra(V(k)
分机）⊤
15：V(k)
ext +=W⊤´Ointra
16：P←W⊙M(k)
17: M(k) += W⊙P
18: Q(k) += PX(k)
分机
19：X(k)
ext += P⊤Q(k)
20：结束
21:▷阶段 2 ⊤：通过块间边界传播进行反向传播（顺序反向）
22：S(M+1)
in ←0dx×dv ▷ 默认情况下最终状态没有损失
23：fork=Mdown to1do
24：S(k)
出←S(k+1)
在 ▷S(k+1)
式中=S(k)
出
25：S(k)
在 += S(k)
输入、输出
26:▷块重复：S (k)
输出=g(k)S(k)
在+B (k)
27: B(k)←S(k)
出
28: ́g (k)←⟨́S(k)
输出，S (k)
在⟩F
29：S(k)
在 +=g (k) ¯S(k)
出
30:▷偏差:B (k) =X⊤
ext(诊断(修复)Vext)
31:m结束←M(k)
C,:,W v←Diag(修正)V(k)
分机
32：X(k)
ext +=W v( ¯B(k))⊤
33：Wv←X(k)
extB(k)
34: ˆm end←row_dot(ˆWv,V (k)
分机）
35：V(k)
ext += Diag(修正) ́Wv
36：M(k)
C,: += 修补
37:▷块退出衰减：g (k) =δ(k)
C
38: δ(k)
C += g(k)
39：结束
40：Sinit←S(1)
在
41：▷阶段 1 ⊤：通过结构化掩模和局部对数衰减进行反向传播（并行）
42:▷辅助求和：row_sum(G) =G1andcol_sum(G) =G ⊤1。
43:fork= 1toMin 并行 do
44：a←a k，b←bk，J←Jk。
45:▷掩模分解：M=DDiag(ρ)A
46:E←Diag(ρ a:b)A (k)
47: ˆD←ˆM(k)E⊤, ˆE←(D(k))⊤ˆM(k)
48: ρa:b += row_dot(E,A (k))
49:▷局部对数衰减:δ i = exp(ui),D i,s =I[s≤i] exp(ui−us),u i =u i−1+ logγa+i−1withu 0 = 0
50：初始化 0：C←0。
51:\u 1:C +=δ (k) ⊙\u δ(k)
52:G←tril( ́D⊙D(k),0)
39

### 第 40 页

**P0040** 53:\u 1:C += row_sum(G)−col_sum(G)
54:初始化 ¯logγa:b←0C
55:fori=Cdown to1do
56:t←a+i−1
57：logγt+=ui
58:\u i−1+=\u\u i
59：结束
60:▷Backproplogγ t = log1p(−αt)
61：堡垒=a，...，bdo
62: ˆα t←−exp(−logγt) ˆlogγt
63: αt += αt
64：结束
65：结束
66：▷分散将重叠梯度添加回全局数组（如果并行则原子添加）。
67：叉子= 1toMdo
68: qak:bk += Q(k)
69: XJk += X(k)
分机
70：VJk+=V(k)
分机
71：结束
72:如果前向传递硬集(ρ1,α1)←(0,0)(如算法5)，则设置¯ρ1←0和¯α1←0。
73：返回（Q，X，V，ρ，α，Sinit）。
C 在序列长度维度上并行化 Delta 网络
第 3 节中的基本 Delta 网络更新具有以下形式
圣 =
（
I−ηtkt−1k⊤
t−1
）
St−1+ηtkt−1v⊤
t ,(C.1)
这对应于方程式中仅投影仪的情况λt = 0。 （3.3）。当岭正则化/权重
启用衰减，方程（3.3）将过渡更改为At = γtI−ηtkt−1k⊤
t−1 且 γt := 1−ηtλt。
对于 γt > 0，可以分解 A t = γt(I−u′
你'
t
⊤) 其中 ′
:=
√
ηt/γt kt−1，并将 ∏γt 吸收为
边界状态和偏差项的累积重新调整。为了清楚起见，下面的 WY 推导
提出仅投影仪的情况λt = 0。
显式减少到仅投影仪的情况（对于 λt> 0）。因为 γt 是标量，所以可以重复使用
WY 机械通过简单的重新缩放而保持不变。 Letc0 := 1 和 ct := ∏t
r=1γr 并定义
～St :=S t/ct， ～ηt := ηt/γt，以及 ～vt :=v t/ct−1。那么衰减递归就等于
仅投影仪重现
〜St =
（
I−~ηt kt−1k⊤
t−1
) ～St−1+ ～ηt kt−1 ～v⊤
t ,o t =c t~ot。
块局部重整化。形成 ct （或 c−1
t−1) 长序列可能下溢/上溢；全部
因此，逐块内核通过重置对数前缀衰减在每个块内应用此重新缩放
to0at 块边界（参见算法 9 和算法 10）。
步长约定。在本附录中，ηt 表示在
排名一更新（例如，方程（3.4）中的 NLMS 步长）。在仅投影仪设置中处理
40

### 第 41 页

**P0041** 这里(λt = 0)，增益βt∈(0,2)对应于
ηt = βt
∥kt−1∥2
2+ε,
按照惯例，当分母消失时，ηt := 0。因此 ηt ≥0 且 √ηt 为
明确的。
C.1 仿射表示和 WY 形式
我们明确定义了仿射结构。写
在 :=I−ηtkt−1k⊤
t−1∈Rd×d,B t :=ηtkt−1v⊤
t ∈Rd×dv,(C.2)
这样递归就变成St =A tSt−1+Bt（仅投影仪；λt = 0）。等式。 (C.2)揭示了
一级结构At =I−utu⊤
t 其中 t :=√ηt kt−1。 Bischof 和 Van Loan (1987) 表明
此类矩阵在块上的乘积可以写为单个 WY 变换（或 UT 变换）。
对于大小为C的块，累积的转移矩阵^A=A t+C···At+1为：
^A=I−UTU⊤,(C.3)
其中U∈Rd×C收集更新向量u，而T∈RC×C是三角混合矩阵。
C.2 分块并行算法
与杨等人类似。 (2024b)，我们有以下三相并行算法。第一阶段
并行计算局部运算符。第 2 阶段在块边界上执行快速递归。
第 3 阶段通过将传播状态与局部因果注意力相结合来实现输出
机制。
41

### 第 42 页

**P0042** 算法7并行DeltaNet训练（前向，λt = 0）
要求：移位键x∈RL×d(withx t :=k t−1)，查询Q∈RL×d，值V∈RL×dv，步长
η ε RL 且 η t ≥ 0（因此定义了 √ η t ），初始状态S init ε R d × d v （默认0）。块尺寸 C
(假设C|L).▷仅投影仪的情况λ t = 0。
1：将输入重塑为M=L/Cchunks:U(k)
原始，Q (k) ∈ Rd×CandV (k) ∈ RC × dv ，其中 η (k) ∈ RC 。
2:▷这里的 t :=k t−1 是移位后的密钥流。
3:▷阶段1：块内预计算（并行）
4:fork= 1toMin 并行 do
5:U(k)←U(k)
原始·诊断(
√
η(k))▷ 缩放关键列
6: 〜V(k)←diag(
√
η(k))V (k) ▷ 缩放值行
7:G(k)←U(k)⊤
U(k) ▷ 格拉姆矩阵
8:L (k)←tril(G(k),−1) +I▷ImplicitT −1
9:Z (k)←solve_triangle(L(k), ～V(k))▷局部值求解
10: ˆB(k)←U(k)Z(k) ▷ 块的累积偏差ˆB
11：结束
12:▷阶段2：块间重复（顺序）
13：S (1)
在←Sinit
14：叉= 1toMdo
15: Γ (k)←solve_triangle(L(k),U (k)⊤
S(k)
in )▷将状态投影到子空间上 (cacheΓ (k))
16：S（k）
出←S(k)
在−U(k) Γ(k) 中 ▷ApplyˆA
17:S (k+1)
在 ←S(k)
out + ˆB(k) ▷ 传播到下一个块
18：结束
19:S决赛←S(M+1)
▷加工后最终状态长度L
20:▷第3阶段：实现输出（并行）
21:fork= 1toMin 并行 do
22:H(k)←Q(k)⊤
S(k)
in ▷ C×dv：查询与传入状态的交互
23:Λ (k)←Q(k)⊤
U(k) ▷ C×C：Query-Key 交互矩阵
24:M (k)←tril(Λ(k),0)▷因果分数包括对角线
25：Y（k）
hist←H(k)−M(k)Г(k) ▷历史贡献（使用缓存的Г(k)）
26：Y（k）
内部←M(k)Z(k) ▷块内贡献（使用cachedZ(k)）
27:O(k)←Y(k)
历史+Y(k)
内部
28：结束
29:▷等价于：O (k) =H (k) +M (k)(Z(k)−Γ(k))其中M (k) = tril((Q(k))⊤U(k),0)。
30：返回（O，S 最终）
第 3 阶段的详细信息。为了有效地实现输出，而无需在每一步都重建 St，
我们将输出分解为两项：yt = y hist
t+y 内
t。对于 chunkk，定义因果分数
矩阵M (k) := tril(Q(k)⊤U(k),0)∈RC×C。
• 历史术语（y hist
t ):对于仅投影仪的 DeltaNet (λt = 0)，块内投影仪产品
已经由单位下三角因子和投影状态求解 Γ(k) = 编码
L(k)−1(U(k)⊤S(k)
in )在第2阶段计算。历史贡献为Y(k)
历史=Q(k)⊤S(k)
in−M(k)Γ(k)。
42

### 第 43 页

**P0043** • 块内项 (y intra
t ):类似地，阶段1计算Z (k) =L (k)−1~V(k)。块内
贡献是因果矩阵乘积Y(k)
内=M(k)Z(k)。
C.3 并行 DeltaNet 训练（向后）
算法8并行DeltaNet训练（向后，λt = 0）
要求：上游梯度 ¯O∈RL×dv 和算法 7 中缓存的前向张量（仅限投影仪，λt = 0）
对于每个块k∈{1,...,M}：未缩放的keysU (k)
原始∈Rd×C，查询Q（k）∈Rd×C，值V（k）∈RC×dv，
步长 η(k) ∈RC，缩放键 U (k) =U (k)
原始诊断（
√
η(k))，缩放值 ∼V(k) = diag(
√
η(k))V(k),
L(k) ∈RC×C,Z (k) =solve_triangle(L(k), 〜V(k)), Γ (k) =solve_triangle(L(k), (U(k))⊤S(k)
中），并且
边界态S (k)
在 εRd×dv 中。
确保：梯度 ̵Q ∈ RL × d， ̅ Uraw ́ RL × d， ̅ V ́ RL × dv， ̅ η ́ RL 和 ̅ Sinit ̅ Rd × dv。
1：将 ˆO 重塑为块 ˆO(k)∈RC×dv，M=L/C。
2:初始化 ̵Q(k), ̵U(k), ̵〜V
(k)
, ˆL(k), ˆS(k)
中，对于所有k， ̅Z(k), ̅ Г(k)←0。
3：置 s(M+1)
进入最终块退出状态的上游梯度（如果损失未使用，则默认为0）。
4:▷伴随恒等式（三角求解）。设L为对角线固定的单位下三角形。若X=L−1B，
5:▷则 ˆB=L −TˆX 且 ˆL−= tril(ˆBX⊤,−1)。
6:▷伴随恒等式（Gram）。如果G=U ⊤U，则 ̵U←̵U+U( ̵G+ ̵G⊤)。
7:▷阶段 3 ⊤：通过输出物化进行反向传播（并行）
8:fork= 1toMin 并行 do
9:▷仅投影仪恒等式：O (k) =H (k) +M (k)(Z(k)−Γ(k))和H (k) = (Q(k))⊤S(k)
在和
M(k) = tril((Q(k))⊤U(k),0)。
10: H(k)←O(k)
11:Λ (k)←(Q(k))⊤U(k)
12:M(k)←tril(Λ(k),0)
13:E (k)←Z(k)−Γ(k) ▷ C×dv
14: M(k)←O(k)(E(k))⊤
15: ˆM(k)←tril(ˆM(k),0)▷M (k) 是因果关系（包括对角线）
16：E(k)←(M(k))⊤O(k)
17：Z(k)←Z(k)+E(k)
18: ψ Г (k)← ψ Г(k)−ψ E(k)
19:▷BackpropH (k) = (Q(k))⊤S(k)
在
20：Q(k)←Q(k)+S(k)
在 ( ́H(k))⊤
21：S(k)
在←S(k)中
在 +Q (k) ́H(k)
22:▷BackpropM (k) = tril(Λ(k),0)andΛ (k) = (Q(k))⊤U(k)
23: Λ(k)←M(k)
24: Q(k)←Q(k) +U (k)( Λ(k))⊤
25: U(k)←U(k) +Q (k) Λ(k)
26：结束
27:▷阶段 2 ⊤：通过块间递归进行反向传播（顺序反向）
28：fork=Mdown to1do
29：S(k)
下一个←S(k+1)
在
30: ▷链接:S(k+1)
式中=S(k)
输出 + ^B(k)
31：S(k)
出←S(k)
下一个
32：B
(k)
←S(k)
下一个
33: ▷偏差: ^B(k) =U (k)Z(k)
43

### 第 44 页

**P0044** 34: U(k)←U(k) + B
(k)
(Z(k))⊤
35: ˆZ(k)←ˆZ(k) + (U(k))⊤ˆB
(k)
36：▷解决：Z（k）=solve_triangle（L（k），〜V（k））
37：V
(k)
求解←solve_triangle(L(k)⊤, ¯Z(k))
38：
(k)
←́〜V
(k)
+ ́~V
(k)
解决
39：L(k)←L(k)−tril
(́~V
(k)
求解(Z(k))⊤,−1
）
40：▷状态：S (k)
输出=S(k)
在−U(k) Γ (k) 中
41：S(k)
在←S(k)中
于+S(k)
出
42: U(k)←U(k)−S(k)
出(Г(k))⊤
43: ⊤⊗S(k)←∫∫(k)−(U(k))⊤⊤S(k)
出
44:▷求解: Γ (k) =solve_triangle(L(k),(U (k))⊤S(k)
在）
45：P(k)
proj←solve_triangle(L(k)⊤, ϋ(k))
46：L(k)←L(k)−tril
(P(k)
proj(Γ(k))⊤,−1
）
47:▷投影输入：P(k)
投影 = (U(k))⊤S(k)
在
48: U(k)←U(k) +S (k)
在 ( ́P(k)
项目）⊤
49：S(k)
在←S(k)中
在 +U (k) ́P(k)
项目
50：结束
51：Sinit←S(1)
在
52:▷阶段 1 ⊤：通过局部结构和输入缩放进行反向传播（并行）
53:fork= 1toMin 并行 do
54: ˆL(k)←tril(ˆL(k),−1)▷diag ofL (k) 是常数 (=I)
55: G(k)←L(k) ▷sinceL(k) = tril(G(k),−1) +I
56: U(k)←U(k)+U(k)(G(k)+(G(k))⊤)
▷backpropG(k) = (U(k))⊤U(k)
57:σ (k)←
√
η(k)
58:▷取消缩放:U (k) =U (k)
原始 diag(σ(k)) 和 ~V(k) = diag(σ(k))V(k)
59：U(k)
原始←¯U(k) diag(σ(k))
60：V(k)←diag(σ(k))V~V
(k)
61:▷列/行点定义：(col_dot(A,B)) i =⟨A:,i,B :,i⟩, (row_dot(A,B))i =⟨Ai,:,Bi,:⟩。
62: σ(k)←col_dot(U(k)
原始, ́U(k)) + row_dot(V(k), ́~V
(k)
）
63:m (k)←I[σ(k) >0]▷逐元素掩码
64: ́η(k)←m(k)⊙
( σ ( k ) / ( 2 σ ( k ) )
）
▷ 当 someηt = 0 时是安全的（例如，boundaryx t =0）：设置 ¯ηt = 0 而不是
形成0/0。
65：结束
66：返回 ́Q, ̅Uraw, ̅V, ̅η, ̅Sinit（将块梯度重塑回长度L）。
D FALCON-2 的并行实现
在本附录中，我们详细介绍了 FALCON-2 的高效并行实现。猎鹰2号
引入逐列自适应学习率 ηt。这意味着每个值通道j∈
{1,...,dv}在状态空间中遵循独特的轨迹，似乎阻止了共享的使用
块转移矩阵。
然而，我们观察到，虽然每列的更新学习率各不相同，但
更新仅由 keyskt 决定，并在所有价值渠道之间共享。由
44

### 第 45 页

**P0045** 利用这种结构，我们可以向量化分块递归，而无需将通道分组为
头，允许以最小的开销实现每维度的自适应性。
D.1 通道无关的矢量化形式
令S t∈Rd×dv 为状态。第 j 列 st,j 的 FALCON-2 更新为：
st,j =s t−1,j+ηt,j kt−1
（
vt,j−⟨kt−1,st−1,j⟩
）
=
（
I−ηt,j kt−1k⊤
t−1
）
st−1,j+ηt,jvt,j kt−1,(D.1)
其中我们假设 ηt,j≥0，因此 √ηt,j 是明确定义的。这明确表明排名一更新
矩阵 I −ηt,jkt−1k⊤
t−1 取决于每通道步长 ηt,j，它决定了块局部
WY系统。为了在 sizeC 的块上并行化，我们利用对偶（Gram）公式。
仅投影仪的形式。为了清楚起见，我们介绍仅投影仪的情况（没有明确的每列衰减
因素）。当在正文中启用脊/权重衰减时，第 j 列更新包括
标量衰减γj,t := 1−λtηj,t：
st,j = (γt,jI−ηt,jkt−1k⊤
t−1)st−1,j+ηt,jvt,jkt−1, γ t,j := 1−λtηt,j。
由于每个值通道独立演化，因此可以通过通道方式消除这种衰减
累积重缩放（相当于，右乘 St 乘以逆累积对角矩阵
衰减），减少回仅投影仪的形式；参见附录 E 针对共享衰变的特殊情况。
Shared Gram Matrix.LetK ∈Rd×Cbe 当前块中（移位的）写入键的矩阵。
核心相关结构由 Gram 矩阵 G=K⊤K∈RC×C 给出。至关重要的是，G
与维度 dv 值无关，并且每个块只需计算一次。对于
通道方面的步长，实际的三角系统通过简单的元素方面的每个通道有所不同
这个共享基础 Gram 的调制。
批量三角求解。每通道步长的变化 ηt,j 调制该 Gram 矩阵。
对于每个通道 j，WY 表示的隐式系统矩阵 Lj 为：
Lj = 特里尔
（
G⊙(√ηj
√η⊤
j ),−1
）
+IC,(D.2)
其中 √ηj∈RC 是块上通道 j 的步长平方根向量，并且 ⊙表示
逐元素乘法（广播）。等价地，让Uj :=Kdiag( √ηj)，我们有
Gj :=U⊤
j Uj =G⊙(√ηj
√η⊤
j ),L j = tril(Gj,−1) +IC。
因为块大小 C 通常很小（例如，C = 64 或 128），所以我们可以有效地执行
批量维度 dv 上的批量三角求解。
D.2 算法
算法 2 以与标量步长直接平行的形式描述了分块前向传递
WY 内核位于附录 C 中，但通过值通道进行批处理。与共享步长情况的唯一区别是单位下三角系统 Lj 是通道相关的（通过 η:,j），因此
三角求解在 j∈{1,...,dv} 上批量执行。
45

### 第 46 页

**P0046** 块读/写公式。令 S in ∈ Rd × dv 为块的传入状态，并令 K , Q ∈ Rd × C
V ∈RC×dv 是块键、查询和值（内部具有因果读后写顺序）
块）。定义布局约定：在块内，我们使用特征主（列主）
布局K，Q∈Rd×C，带有沿列的标记。具体来说，如果K(m),Q(m)∈RC×敢为标准
chunkm 的时间主切片，然后我们设置 K:=K ⊤
(m) 且 Q:=Q ⊤
（米）。我们保持V ∈RC×dv
时间主数，类似地，每个通道的步长为 ηεRC×dv。
G:=K ⊤K∈RC×C,M:= tril(Q ⊤K,0)∈RC×C,H:=Q ⊤Sin∈RC×dv,
以及预计的输入状态P:=K ⊤Sin∈RC×dv。每个通道的步长为 ηεRC×dv
且Σ:= √η，为每个通道j定义单位下三角
Lj :=I C + tril
（
G⊙(Σ:,jΣ⊤
:,j),−1
）
。
在整个过程中，我们包括对角线（先写后读）viatril(·,0)。
未缩放的分数。请注意，Mi 是故意使用未缩放的键 K 形成的。所有 √η 因子
被吸收到下面的系数矩阵B中；等价地，对于每个通道j,M
（
Σ:,j⊙·
）
=
特里尔
（
Q⊤Kdiag(Σ :,j),0
）
(·)。
然后，对于每个通道j，求解
zj :=L−1
j
（
Σ:,j⊙V:,j
）
εRC,γ j :=L−1
j
（
Σ:,j⊙P:,j
）
εRC。
将每个通道的解堆叠为列给出矩阵 Z, Γ ∈ RC × dv 其中 Z :,j = z j 和
γ:,j =γ j。定义系数矩阵B∈RC×dv为
B:,j :=Σ :,j⊙(zj−γj)，等价于B:=Σ⊙(Z−γj)。
然后块输出和块退出状态
O=H+MB∈RC×dv,S out =S in +KB∈Rd×dv。
伪代码。正文中的算法 2 提供了仅用于投影仪的伪代码（λt = 0）
向前传球；本附录重点介绍推导、复杂性和 λt>0 扩展。
效率分析。对于每个块，形成共享的 Gram 和分数矩阵的成本为 O(dC2)
K⊤K和Q⊤K。构建特定于通道的系统{Lj}并执行批处理
三角求解costO(dvC2)。通过MB实现块输出也花费O(dvC2)，并且
块退出状态更新KBcostsO(ddvC)。与简单的每通道实现相比
这将需要每个值维度的显式×doperators，这将速率依赖性限制为
小型C×C系统。然而，O(dvC2)项对于largedv来说可能是很大的，激励
附录 E 中的共享动态 Lite 变体。
包括脊/权重衰减 ( λt> 0)。算法 2 是针对仅投影仪的情况编写的 λt = 0。
然而，在正文中，FALCON-2 默认使用 λt > 0，这会产生乘法
每个价值通道上的收缩。对于每通道步长 ηt,j 和标量脊λt，第 j 个
列演变为
st,j =
（
(1−λtηt,j)I−ηt,jkt−1k⊤
t−1
）
st−1,j+ηt,jvt,jkt−1.
46

### 第 47 页

**P0047** 定义每通道衰减γt,j := 1−λtηt,j 和累积乘积c0,j := 1, ct,j := ∏t
s=1γs,j,
andD t := Diag(ct,1,...,ct,dv)。然后按列重新缩放状态 〜St :=S tD−1
t 满足
仅投影仪重现：
〜st,j =
（
I−~ηt,jkt−1k⊤
t−1
) ～st−1,j+ ～ηt,j ～vt,jkt−1, ～ηt,j :=ηt,j/γt,j, ～vt,j :=v t,j/ct−1,j。
等价地，我们可以在重新调整的变量 eta 和 V 上运行算法 2（工作在
〜S域），然后通过Ot = 〜OtDt和St = 〜StDt恢复原始输出和状态（即，
每个通道上按元素乘法 byct,j)。
对数空间注释。计算 fp32 中的 logγt,j = log1p(−λtηt,j) 并钳制 λtηt,j≤1−εγ，使得 γt,j > 0。
在分块内核中，不要在整个序列上形成全局乘积ct,j（或exp(±zet,j)）；相反，
在每个块边界重置日志前缀并仅使用块本地前缀（算法 9）。
47

### 第 48 页

**P0048** 算法9并行化FALCON-2（Chunk-wise Forward，λt>0）
要求：移位键K ∈RL×d，查询Q ∈RL×d，值V ∈RL×dv，每通道步长
ηεRL×dv 且 ηt,j≥0, 岭系数 λεRL
>0，clampεγ>0，块大小C（假设C|L），
初始状态S init（原始比例；默认0）。
Ensure: OutputsOfor the decayed per-channel update withγt,j = 1−λtηt,j (clamped to be> 0),
使用块局部重整化。
1：将长度-L轴划分为M = L / C连续块并使用相同的块布局
约定如算法 2。
2:S in←Sinit
3:形式= 1,...,Mdo
4：切片K(m)，Q(m)εRC×d，V(m)εRC×dv，η(m)εRC×dv，且λ(m)εRC。
5:形成列主K:=K ⊤
(m)∈Rd×CandQ:=Q ⊤
(m)∈Rd×C。
6:α←η(m)⊙(λ(m)1⊤
dv)▷ α t,j =λtηt,j
7:α←min(α,1−ε γ)▷确保γ t,j = 1−αt,j >0
8:logγ←log1p(−α)▷ C×d v;推荐 fp32
9: ˆη←η(m)⊙exp(−logγ)▷ ˆηt,j =ηt,j/γt,j
10:▷块本地日志前缀衰减（在块边界重置）
11:u 0,:←0dv
12:堡垒= 1,...,Cdo
13:u t,:←ut−1,:+ logγt,:,δt,:←exp(ut,:)▷ δ t,j = ∏t
r=1γr,j；推荐 fp32
14：结束
15: 重新调整后的值： ˆVt,:←V(m),t,:⊙exp(−ut−1,:)fort= 1,...,C ▷ ˆvt,j =v t,j/δt−1,j
16:▷仅投影仪 WY/Gram 步进( ˆV, ˆη)（与算法 2 的代数相同）
17:Σ← √ˆη▷ C×d v
18:G←K ⊤K,M←tril(Q ⊤K,0)
19:H←Q⊤Sin,P←K⊤Sin
20:BuildL j = I C + tril
（
G⊙(Σ:,jΣ⊤
:,j),−1
）
forj= 1,...,d v。
21：Z←批量求解Tri
（
{Lj}dv
j=1, ˆV⊙Σ
）
22: ←BatchedSolveTri
（
{Lj}dv
j=1,P⊙Σ
）
23：B←Σ⊙(Z−Γ)
24: ^O(m)←H+MB, ^Sout←Sin +KB
25：▷重新调整回原始（衰减）变量
26：O（男）
t,: ←ˆO(m)
t,: ⊙δt,: 堡= 1,...,C
27:S in←ˆSout Diag(δC,:)
28：结束
29:returnconcatenated outputsO.
E FALCON-2-Lite：高效共享动力
在附录 D 中，我们推导了 FALCON-2 的矢量化并行形式。虽然理论上是最优的，
该公式需要每个值维度 j∈{1,...,dv} 都有一个独特的学习率轨迹。
这造成了计算瓶颈：隐式逆矩阵 T（或 Cholesky 因子
L) 取决于学习率。因此，完整的 FALCON-2 需要解决不同的问题
48

### 第 49 页

**P0049** 每个块的大小为 C×C 的三角系统。 Sinedv（模型宽度）通常很大
（例如，4096），这会阻止使用高效的块矩阵乘法并导致高内存
带宽使用情况。
在本节中，我们将介绍 FALCON-2-Lite，这是一种硬件高效的变体，它强制执行共享
所有价值渠道的自适应学习率。此约束减少了不同的数量
必须从 dv 到 1 构建的 C×C 三角形系统，支持每个块的单个多右手边三角形求解，同时保持跨值共享每步投影仪动态
渠道。
渐进地，应用状态更新仍然需要 O(ddvC) 并求解三角系统
dv 右侧成本O(dvC2)。主要的节省来自于消除了
构造和分解不同的 C×Csystems：Lite 对每个块使用一个共享系统，并且使用一个
具有许多右侧的高通量求解。
E.1 Formulation: Scalar vs. Vector Learning Rates
在完整的 FALCON-2 中，状态更新由向量速率 ηt∈Rdv 驱动。在 Lite 版本中，我们
将其限制为标量 ηt ∈ R，从输入特征 kt−1 的全局能量导出：
ηt = βt
∥kt−1∥2
2 +λt +ε, β t∈(0,2), λt≥0, ε≥0,(E.1)
其中βt是可学习的标量NLMS增益，λt是岭系数（默认λt>0），ε是一个小的
稳定剂（参见方程（3.4））。状态矩阵St∈Rd×dv的自回归更新规则简化
至：
圣 =
（
(1−ηtλt)I−ηtkt−1k⊤
t−1
）

在
St−1+ηt kt−1v⊤
t , γ t := 1−ηtλt.(E.2)
至关重要的是，转移矩阵 At 现在在 St 的所有列之间共享。这意味着虽然
不同的特征存储不同的内容（值），它们共享相同的动态（写入/忘记
速度）。
包括权重衰减。FALCON-2 默认使用 λt > 0，从而引入收缩因子 γt :=
1−ηtλt。对于下面的对数空间缩减，我们要求 γt> 0（等效地，ηtλt< 1）。在我们的
默认 RMSNorm 缩放（所以∥kt−1∥2
2≈d) 且 λt∈[0,1], 方程(E.1) 意味着 ηtλt ≲βt/(d+ 1)<1
因此 γt∈(0, 1] 自动地。更一般地（并且在有限精度下），我们限制衰减
分数αt :=ηtλt asαt←min(αt, 1−εγ)，即γt←max(1−ηtλt,εγ)，使得γt≥εγ> 0。
简单边界约定 k0 = 0（因此 1 = 0），我们取 η1 = 0 且 γ1 = 1（无写入/衰减），
匹配更新 start att = 2 的主文本约定。因为 γt 是标量，所以可以
通过重新缩放来减少仅投影仪情况下的衰减重现。 Letc0 := 1 和 ct := ∏t
r=1γr,
并定义 〜St :=St t/ct （因此在写后读取下，读取满足=ct〜ot）。然后更新就失效了
相当于在上运行仅投影仪的 WY 内核
～ηt :=ηt/γt, ～vt :=v t/ct−1,
并重新调整输出 byct。算法 10-11 明确地实现了这种减少。
数值稳定性。仅投影仪 WY 内核的代数简化通过以下方式重新调整目标
逆累积衰减， ∼vt =v t/ct−1withct = ∏
r≤tγr。虽然在精确算术上是正确的，但在有限的情况下
49

### 第 50 页

**P0050** 当 γt 明显低于 1 时，精度 ct 可能会变得极小，使得 c−1
t−1= exp(−zet−1)
当按exp(zt)重新缩放时，会发生溢出并产生∞×0模式（和NaN）。
稳定的块局部重整化。为了避免在整个序列上形成 exp(±zt)，我们的
实现在长度C的每个WY块内执行相同的减少。对于一大块
[a,b ]（其中 b = a+C−1），定义局部对数前缀衰减su0 := 0and ui := Σi−1
r=0 logγa+r 对于
i = 1,...,C，其中 δi := exp(ui)。我们在重新缩放的值上运行仅投影仪的 WY 内核
ˆva+i−1:=v a+i−1exp(−ui−1) =v a+i−1/δi−1 和步长ˆηt :=ηt/γt （计算 logγt = log1p(−αt)
在 fp32 中，αt := min(ηtλt, 1−εγ)，则 ˆηt = ηt exp(−logγt))。输出和块退出
状态通过前向局部衰减重新调整：o a+i−1= δiˆoa+i−1 和 S b = δC ˆSb。这是
代数上等价于全局归约，但指数范围为 O(C) 而不是 O(L)。
E.2 通过共享 WY 分解并行实现
FALCON-2-Lite 使用在所有值通道之间共享的单个标量步长 ηt。因此，
在每个块内，WY 因子在 S 的列之间共享：每块系统矩阵
L(k) 对于所有 dv 列都是相同的。这正是附录 C 的设置（仅限投影仪，
λt = 0)，其中 WY 实现产生一个单位下三角矩阵 L(k)∈RC×Cper
chunk 和所有三角形求解都是带有 dv 右手边的标准多 RHS 求解。
前向/后向重用。通过共享动态，块并行注意力前向传递是
恰好是算法 7，而通过 WY 机器的向后传递是算法 8。
FALCON-2-Lite 中的附加成分是步长 ηt 不是自由输入：它是生成的
通过 NLMS 归一化
ηt = βt
∥xt∥2
2 +λt +ε,x t :=k t−1,(E.3)
默认值 λt > 0 引入了额外的标量路径 γt = 1−ηtλt。反向传播
因此，包括通过 ηt 和累积衰减的链式法则步骤（算法 11）。
E.3 算法
Forward.Compute( ηt,logγt)（带钳位）并在块本地重整化输入上运行仅投影仪的 WY 内核：在每个块内计算本地前缀esui 和 δi = exp(ui)，使用
该块内的 ^ t = η t / γ t 和 ^ v t = v t / δ i−1 ，并通过 δ i 重新调整输出/状态（算法 10）。
50

### 第 51 页

**P0051** 算法 10FALCON-2-Lite（前向）
要求：移位键x∈RL×d(boundaryx 1 =0)，查询Q ∈RL×d，值V∈RL×dv，增益
βε(0, 2)L, 岭λεRL
≥0，稳定器ε>0，夹具εγ> 0，虚拟εη> 0，块大小C（假设
C|L)，初始状态Sinit。
确保：衰减递归的输出O，其中 ηt = βt/(∥xt∥2
2 +λt +ε) 且 γt = 1−ηtλt
（夹紧）。
1:计算η1←0,且fort≥2:dt←∥xt∥2
2 +λt +ε,ηt←βt/dt（范数/比率为 fp32）。
2:α原料
t ←ηtλt,αt←min(αraw
t ,1−εγ)fort≥2。
3：logγ1←0且fort≥2：logγt←log1p(−αt)(fp32)，γt←exp(logγt)。
4： ˆη1←εη（虚拟；自 x1 =0 起无影响），并且 fort≥2： ˆηt←ηt/γt = ηt exp(−logγt)。
5：划分为M=L/Cchunks[a k,bk] = [(k−1)C+1,kC]。 SetSin←Sinit。
6:叉= 1,...,Mdo
7：a←a k，b←bk。
8:u 0←0▷本地日志前缀在块内衰减
9:fori= 1,...,Cdo
10:u i←ui−1+ logγa+i−1,δi←exp(ui)▷fp32
11：结束
12: ˆVa+i−1←Va+i−1exp(−ui−1)fori= 1,...,C ▷ ˆv=v/δi−1
13：使用 init 在长度 C 序列（x a:b, Qa:b, ^Va:b, ^etaa:b）上运行仅投影仪的 WY 内核
Sin（算法 7，L=C），产生（ ˆOa:b, ˆSout）。
14:O a+i−1←δi ˆOa+i−1fori= 1,...,C。
15:S in←δC ˆSout 传播原始块退出状态
16：结束
17：返回O。
向后。算法 8 提供将 (x , η ) 视为独立的梯度。 FALCON-2-Lite
另外通过 NLMS 映射进行反向传播 ηt = βt/(∥xt∥2
2 +λt +ε) 并通过
chunk-locallog-decay 重整化（局部前缀esui和δi）用于处理γt（算法11）。
算法 11FALCON-2-Lite（向后）
要求：上游梯度 ¯O，来自算法 10 的每个块的块本地缓存：本地前缀
{ui,δi}C
i=0，logγak:bk,( η,β,λ)，clamp maskmt = I[ηtλt < 1−εγ]，并且 WY 缓存运行
仅投影仪内核 on(xak:bk,Qak:bk, ˆVak:bk,ˆηak:bk)。
确保：梯度（Q，X，V，β，λ，Sinit）。
1：将所有梯度初始化为零。设置边界S(M+1)
在←0。
2:fork=M,...,1do▷反转块
3:a←a k,b←bk,令i=t−a+ 1∈{1,...,C}为局部索引。
4:初始化 ̅u i←0fori= 0,...,C, ̅logγt←0和̅ηt←0fort=a,...,b。
5:▷取消缩放输出：O t =δi ˆOt
6:堡垒=a,...,bdo
7:i←t−a+1
8: ˆOt←δi ˆOt
9: ˆu i +=⟨ˆˆOt, ˆOt⟩▷ ∂δ i/∂ui =δi
10：结束
11:▷边界：下一个块看到S (k+1)
in =δC ˆSout
51

### 第 52 页

**P0052** 12: ˆSout←δC ˆS(k+1)
在
13: ˆu C +=⟨ˆˆ南, ˆ南⟩
14：在这个长度为 C 的块上向后运行仅限投影仪的 WY（算法 8，其中 L = C），使用
上游( ˆOa:b, ˆSout), 得到( ˆQa:b, ˆxa:b, ˆVa:b, ˆetaa:b, ˆS(k)
中）。
15:▷取消缩放值：^Vt =V t exp(−ui−1)
16:堡垒=a,...,bdo
17:i←t−a+1
18：s←exp(−u i−1)
19: ˆVt +=s ˆVt
20: ˆu i−1+=−⟨sˆVt,Vt⟩
21：结束
22:▷取消缩放步长：^\eta t =\etat exp(−logγt)
23：堡垒= max(a,2),...,bdo
24:g −1←exp(−logγt)
25: ˆ t t + = g−1 ˆ t t
26: ˆlogγt +=−(ηtg−1) ˆˆηt
27：结束
28:▷Backprop局部前缀和：u i =u i−1+ logγa+i−1
29：fori=Cdown to1do
30:t←a+i−1
31：ift≥2then
32：logγt+=ui
33：如果结束
34：\u i−1+=\u i
35：结束
36:▷Backproplogγ t = log1p(−αt)其中αt = min(ηtλt,1−εγ)
37:堡垒=最大(a,2),...,bdo
38: ́α t←−exp(−logγt) ́logγt
39: ́α t←mt ́αt ▷ 夹紧时梯度为零
40:¯η t +=λt ¯αt
41: ́λt += ́t ́αt
42：结束
43:▷Backpropη t =βt/(∥xt∥2
2 +λt +ε)
44:堡垒=最大(a,2),...,bdo
45:d t←∥xt∥2
2 +λt +ε
46: ́βt += ́ηt/dt
47: ́dt←−βt ́ηt/d2
t
48：xt += 2 dt xt
49: λt += dt
50：结束
51:▷边界常数ˆη 1 :=εη 和logγ1 := 0 不带梯度。
52：结束
53:return( Q, x, V, β, λ, Sinit) 且 Sinit = S(1)
在 .
E.4 复杂性分析
表 5 总结了每个块的成本。共享动态消除了依赖于速率的系统
在完整的 FALCON-2 中，构建开销可缩放为 O(dvC2)（构建/分解 dv 不同
C×C三角系统）。剩余的三角解仍按 O(dvC2) 缩放，因为状态
52

### 第 53 页

**P0053** has dv columns, but it is a single highly optimized multi-RHS solve, which is substantially more
实践中对 GPU 友好。
表 5 每个块的复杂性（块大小 C、特征 dimd、值 dimd v）。
组件 FALCON-2（完整） FALCON-2-Lite
格拉姆矩阵O(dC 2)O(dC 2)
速率相关系统构建(L)O(d vC2)O(C 2)
三角求解 (L−1withd v RHS)O(d vC2)O(d vC2)
状态更新投影O(dd vC)O(dd vC)
FALCON-2-Lite 通过强制执行消除了构建和分解不同 C×C 系统的需要
共享动态。剩余的三角解仍然按 O(dvC2) 缩放，但它变成了单个
高通量多 RHS 求解，在实践中对 GPU 更加友好。
F 通过 ParallelFlow 高效训练 FALCON-3
虽然 Mamba-2 中使用的 SSD 框架为 1 级更新提供了有效的算法，
FALCON-3（滑动回归）引入了对 sizeB 历史窗口的依赖。这个
有效地将系统转变为 B 级循环系统。无需求助即可有效地进行训练
为了减慢状态矩阵的具体化或简单的顺序处理，我们采用 ParallelFlow
框架（Cirone 和 Salvi，2025）。
在本附录中，我们首先提供 ParallelFlow 的独立介绍，处理 Linear
注意力作为矩阵值控制微分方程（CDE）。然后我们明确地映射
FALCON-3 更新了这种形式主义的规则，表明它构成了
低阶 Delta 规则可通过张量Invalgorithm 求解。
F.1 背景：矩阵值 CDE 和低阶驱动程序
标准线性递推可以被视为连续时间过程的离散化。让
St∈Rd×dv 是隐藏状态。 We adopt the left-multiplicative convention and model its evolution
作为由 ω 和 xi 驱动的矩阵值 CDE：
dSt =dω tSt +dψt,(F.1)
where ωt∈Rd×dand ξt∈Rd×dv are matrix-valued paths. The solution over an interval[s,t ]factors
通过线性传播器（流）Pt←s：
St =P t←sSs +
∫ t
s
Pt←rdψr,其中Pt←s=I+
∫ t
s
dωrPr←s.(F.2)
这种分解将时间动态 (P) 从计算中分离出来。并行流
通过将序列划分为块 [tk−1,tk] 来并行化。系统计算本地
并行传播器和每个块的累积输入注入，然后通过
全局关联扫描。
53

### 第 54 页

**P0054** 转置等价形式。ParallelFlow 通常以共享右因子形式呈现
dωt =A tB⊤
t dt, dψt = ～AtB⊤
t dt，而 FALCON-3 在我们的左乘递归下最自然地以转置等效共享左因子形式编写
St+1 =St+AtB⊤
t St +At ～B⊤
t。
通过调换低秩因子/状态更新恒等式，这两个视图在代数上是等价的。
我们在整个附录中使用共享左因子形式，因为滑动回归更新
自然地共享写入特征的窗口矩阵。
低阶驱动器。核心难点在于计算传播器Ps→t，它通常是一个
昂贵的矩阵 ODE。然而，如果驾驶员拥有低等级，则有效的计算是可能的
结构。我们假设 Rdrivers 具有共享的左因子：
dωt = ⃗At⃗B⊤
t dt, dxi t = ⃗At
～⃗B
⊤
t dt,(F.3)
其中 ⃗At,⃗Bt∈Rd×Rand ～⃗Bt∈Rdv×R。这种结构允许有效计算、紧凑
流的表示。对于离散标记，在前向欧拉离散化下（步骤
大小吸收到⃗B，〜⃗B，相当于Δt= 1)，更新变为：
Stk+1 =S tk + ⃗Atk
⃗B⊤
tk Stk + ⃗Atk
～⃗B
⊤
tk.(F.4)
tensorInv 算法。在一段长度上求解这个低秩系统 Lc 可以减少
到三角张量反演。设C∈R(Lc×R)×(Lc×R)是表示因果相互作用的张量
等级成分之间。对于索引 s,t ∈ {1,...,Lc} 和ranki,j ∈ {1,...,R}：
[C]s,j
t,i =δs,tδi,j−I(s<t)
[
⃗B⊤
⃗As
]
我,j
.(F.5)
Cirone 和 Salvi (2025) 表明，传播器可以通过应用结构化
张量收缩乘积下的逆张量D =C−1，从而避免显式的densedx×dx
块传播者。在我们的设置中，计算由 (LcR)×(LcR) 结构控制
因果求解以及涉及 dx 和 dv 的矩阵乘法；压缩渐近
O(L2
因此，cR +d) 在这里具有误导性，因为它隐藏了主要的与维度相关的术语。
tensorInv 输出（算法 4 中使用的对象）。在实践中，我们不会具体化
全逆张量D.相反，ParallelFlow 将 C−1 应用于对应于的两个右侧
低级别司机。对于长度Lc和rankR的块，堆叠（展平时间×等级）离散
司机作为
⃗A∈Rdx×(LcR)，⃗B∈Rdx×(LcR)，～⃗B∈Rdv×(LcR)。
让 ⃗M∈{0, 1}(LcR)×(LcR) 表示严格因果块掩码，对于之前的交互，它是 1
次s<tand0否则；等价地，在(t,i)索引中，它正是使用的指标I(s<t)
在上面C的定义中。特别地，在平坦化时间×等级之后，相同时间等级的分量
不要在三角解内相互作用。然后可以写出块局部三角系统
紧凑地作为
C=Id−⃗M⊙(⃗B⊤⃗A),
54

### 第 55 页

**P0055** 和由tensorInvare返回的两个解
⃗W:=C −1⃗B⊤εR(LcR)×dx, ⃗U:=C −1～⃗B
⊤
εR(LcR)×dv.(F.6)
我们将这些解决方案表示为
(⃗W, ⃗U) =tensorInv( ⃗A, ⃗B, ～⃗B)。
给定一个传入的块边界状态Sin∈Rdx×dv，定义⃗Z := ⃗U + ⃗W Sin∈R(LcR)×dv。然后
块退出状态是
Sout =S in + ⃗A⃗Z,(F.7)
这是算法 4 使用的形式（通过处理额外的标量衰减γt）
chunk-local renormalization).
F.2 将 FALCON-3 映射到 ParallelFlow
我们现在明确地将第 4 节中导出的 FALCON-3（滑动回归）更新规则映射到此
低阶 CDE 框架。
FALCON-3 Recurrence.Recall 窗口大小为 B 的小批量更新（忽略
标量衰减γt = 1−ηtλt，我们的分块实现通过相同的块本地处理
整个过程中使用的对数衰减重整化）：
圣 =
（
I−ηt C(B)
t
）

转移矩阵
St−1+η t N(B)
t
输入注入
=

I−ηt
转氨酶
Σ
j∈It
kj−1k⊤
j−1

 St−1+ ηt
转氨酶
Σ
j∈It
kj−1v⊤
j ,(F.8)
其中B t :=|It|≤B, ́C(B)
t :=C (B)
t /Bt 和 ́N(B)
t :=N (B)
t/Bt。
如第 3 节中所示，可以通过定义 xj :=phi(kj−1) 并替换 将其写在特征空间中
kj−1byx j 自始至终，并且非核化情况具有 phi 恒等式。
驱动程序的识别。将其映射到方程式。 (F.3) 在没有边界模糊的情况下，我们定义
使用活动索引 setIt ={j|max(2,t−B+ 1)≤j≤t} 的窗口矩阵。令Bt :=|It|≤B
并不断地对它的元素进行排序。通过堆叠定义窗口键/值矩阵
仅有效索引：
⃗Kt :=
[
kj1−1kj2−1...k jBt−1
]
εRd×Bt,(F.9)
⃗Vt :=
[
vj1 vj2 ...v jBt
]
εRdv×Bt,(F.10)
其中(j 1,...,jBt)枚举It。有了这些定义，
乙(B)
t = ⃗Kt⃗K⊤
t,N (B)
t = ⃗Kt⃗V⊤
t , C(B)
t = 1
转氨酶
⃗Kt⃗K⊤
t , N(B)
t = 1
转氨酶
⃗Kt⃗V⊤
t ,
55

### 第 56 页

**P0056** 完全适合所有人。 （等效地，可以用零列填充⃗Kt，⃗Vt，直到宽度B；乘积
以上均未改变。）代入方程（F.8）得到规范的左乘低秩
形式
St =S t−1+
(ηt
转氨酶
⃗Kt
）

⃗在
(−⃗K⊤
t)
⃗B⊤
t
St−1+
(ηt
转氨酶
⃗Kt
）

⃗在
⃗V⊤
t
～⃗B
⊤
t
(F.11)
因此，使用 ParallelFlow 秩参数 R =Bt≤B（或补零后固定秩 R =B），
方程中的驱动器(F.3) 是
⃗At = ηt
转氨酶
⃗Kt∈Rd×Bt, ⃗Bt =−⃗Kt∈Rd×Bt, ˜⃗Bt = ⃗Vt∈Rdv×Bt.
算法 4 中，对式（4.15）进行正衰减重整化后，只需将 ηt 替换为 ^ηt
并通过公因子(δ(k)重新调整整个step-tvalue块
i−1)−1在块k内。
复杂性。因为 FALCON-3 可以精确地转换为具有 B 级驱动程序的 CDE，所以我们可以利用
张量反转算法。对于我们实验中使用的小滑动窗口（例如，B = 4），
B 级开销在实践中是适度的，并且仍然比 fullO(L2)attention 便宜得多
同时避免densex×dx状态传播。这一推导证实了 FALCON-3 的优势
来自并行扫描方法的对数线性缩放，同时保持表达能力
滑动窗口回归器。
56
